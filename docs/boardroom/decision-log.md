# HelloKahwin — decision log

One entry per decision: date, decision, basis, prediction. Reviewed at later
meetings against what actually happened. Newest first.

## 2026-09-01 — Sprint 05 planning: the click, not the impression

175. **SPRINT 05 IS *BUILD WHERE THE CLICK IS* — 19 items, 81 points, NOTHING CUT.** Content 20, design 31, technical SEO 8, risk 9, platform 13. Imported to the tracker and **read back rather than trusted from the import's own report**: `sprint list --sprint 5` returns `0/81 points, 19 items`. **The basis is that Sprint 04 was called *Earn the click* and the click did not move.** GSC 28d, 30 Aug → 31 Aug: clicks 99 → **109**, impressions 6,693 → **7,760**, position 12.4 → **11.8**, sitewide CTR 1.48% → **1.40%**. Sprint 04 executed correctly — 69/69, every item verified against production — but **presentation was never what suppressed CTR**, and the page-level split makes that sharper than SEO-11's census did. Seven days to 31 Aug, matched positions: the `mas kahwin` state series + `walimatul urus` + the hantaran ratio pages returned **21 clicks from 3,062 impressions (0.69%) at position ~7.0**; `checklist-kahwin`, `/dewan-kahwin/`, `goodies-kahwin`, `nisbah-hantaran` and `bajet-kahwin` returned **17 from 285 (5.96%) at position ~7.4**. Same site, same design, same brand. **Roughly 40% of the impression base is structurally unclickable however well it ranks.** ⚠ **This DISAGREES with Sprint 04's retro**, which named the composition work as Sprint 05's theme. It is a track here (31 of 81 points) and it is not the sprint: composition answers the owner's premium complaint and does not answer the click problem, and the click problem is measured at 12.2× (SEO-11, Fisher exact p = 0.00002).

176. **⚠ OWNER CONFIRMED, 01 SEPT: THE PHOTOGRAPHER PERMISSIONS ARE OBTAINED.** *"Yes I got the permissions, proceed with the full scope."* This was **decision 161's mandated follow-up**, brought back by the CEO as that decision required — the standing proof being decision 50, where an assignment nobody audited surfaced only because a later meeting happened to re-pull the same query. **RIGHTS-02 changes purpose rather than shrinking:** planned on 30 Aug as enumeration to support a negotiation the owner was about to run, it becomes a **record of rights the company now holds**, grouped by photographer or source, with every group marked covered / institutional / unknown-and-named. Nothing is deleted by it and **nobody is contacted by us** — outbound contact is carve-out 3 and the owner has taken it.

177. **🔴 DECISION 167 WAS DECIDED ON 30 AUG AND NEVER EXECUTED. FILED AS RIGHTS-03.** It ruled: *"the Getty/iStock image and the press photograph belong to INSTITUTIONS, not to photographers who know us… Those two come down, with UNDO pushed first."* **RIGHTS-02 was then deferred by the owner's 31 Aug redirect and this sub-commitment went with it** — `docs/work-done/aug-30-2026-session-01/` holds no entry for it. **This is decision 50's pattern exactly**, and the pattern's own recorded lesson: *a sub-commitment that lived inside a parked or re-scoped item disappears without ever being decided against.* It was found by the open-assignment audit, which is what that audit exists for. **Decision 176's permissions do not reach these two** — "photographers have good relationships with us" was never an argument that reaches Getty, and the CEO said so when 167 was written. Two files; the only genuine legal exposure the company carries; UNDO pushed before the first delete.

178. **CONT-13's GATE IS CORRECTED IN THE BRIEF BEFORE RE-IMPORT — TWO OF ITS THREE TESTS ARE SUPERSEDED BY ITS OWN SPRINT.** Re-importing Sprint 04's brief unchanged would have re-imported a criterion that sprint disproved. **(1) Volume:** "≥100 impressions/28d or ≥100 monthly volume" is replaced by **decision 170's ≥220 monthly searches** for document intent (a 10-clicks/month bar ÷ the 4.55% the class achieves). **(2) The AI Overview is NOT a selector**: the brief's test 2 said *"No AI Overview on the live SERP, verified by `serp-overview`"*, and **decision 169 killed that** — AIO presence reads p = 0.102 across all 84 rows, 94% of number-intent queries carry one **but so do 79% of document-intent ones**, so the feature sorts nothing; `intent_class` survives every re-cut at p = 0.000003–0.000025. Record the AIO as advisory; never select or reject on it. **(3) Unchanged:** not already owned by a sibling page. **The general lesson: a carried-forward brief is a snapshot of what was believed when it was written**, and a sprint that disproves its own premise leaves the brief behind unless someone edits it.

179. **DES-17 IS A SEPARATE ITEM FROM UI-13: THE H6 RULE IS WRITTEN BEFORE ANYONE BUILDS IT.** DES-03 §5.3 asserts *"The specified homepage carries the diversity rule — see H6 in §7"*, and **H6 does not exist**: its four `\bH6\b` matches are three base64 fragments inside embedded font data plus the dangling reference itself, and §7 is the state set ("diversity" 0 occurrences, "categor" 0). **No builder could have implemented it**, which is why the homepage still shows one category thirteen times. **This is Sprint 04's central finding in its purest form** — in the same spec, by the same author, the parts written as enforceable rules (hero eligibility, R8a/R8b/R8c) shipped exactly, and the parts written as prose and a drawing (this rule, the 300px rail, the in-article TOC) did not ship at all. DES-17's DoD therefore requires the rule to be stated as a constraint **a script can evaluate**, with the test that UI-13 must not have to interpret prose to implement it.

180. **THE FAQ-SCHEMA FIGURE IN `ceo-memory.md` WAS STALE AND IS CORRECTED: 39 OF 85, NOT 31.** Live census across every article URL in the sitemap, 01 Sept — 46 carry `FAQPage`, 39 do not, 1 fetch failure. The corpus grew since the figure was written. **The missing set includes pages we actively rank on**: `/idea-dan-nasihat/dewan-kahwin` (142 impressions at 2.82%, one of our better converters), `/idea-dan-nasihat/garden-wedding` (the 28%-of-impressions page), `/hantaran-mas-kahwin/hantaran-tunang` (291 impressions), plus `borang-nikah`, `rukun-nikah` and `syarat-sah-nikah`. Filed as SEO-13 with an explicit prohibition on inventing questions to raise the count. **Second time in three meetings a carried figure has been wrong when re-measured** (the first was the garden-wedding credit count) — the standing rule to *re-measure the carried-forward list rather than read it* has now paid twice.

181. **DECISION 168 IS RE-DATED: THE ENFORCE-CREDIT-EVERYWHERE PROGRAMME IS A SPRINT 06 QUESTION.** D168 made it a Sprint 05 question *once RIGHTS-02 had sized it*. **RIGHTS-02 runs in Sprint 05, so it cannot also be sized by it** — the condition and the sprint were the same sprint, which is how a deferral quietly becomes a stall. Written into Sprint 05's `out_of_scope` with the reason, so the plans index stops reading as though someone else owes an answer. Same failure D168 was itself written to fix.

182. **⚠ ELEVENTH INSTANCE OF THE VERIFICATION SHAPE — AND THE FIRST DIAGNOSIS OF IT WAS ALSO WRONG, WHICH IS THE MORE USEFUL HALF.** `grep -oiF "artikel"` returned **0** on a live page whose URL contains `/artikel/`; it also returned 0 for `REKOD` and `SUMBER`, which are present **×24 and ×20**. Reported as an absence it would have said *the article rail's scaffolding does not exist*, sending UI-17 to rebuild markup that is already shipped. **THE CEO'S FIRST DIAGNOSIS WAS CONFIDENT, SPECIFIC AND WRONG:** it blamed the file being 145,191 bytes on one line with no terminator — *"grep classified it as binary"* — and wrote `-a` into a new helper script as the fix. **`-a` does not fix it.** That was caught only because the helper was **run against the case it was written for** before being trusted, and it still returned 0:

  | command | result |
  |---|---|
  | `grep -oaiF artikel <page>` | **0** — still zero *with* `-a` |
  | `grep -oaF artikel <page>` | 89 |
  | `grep -oai artikel <page>` | 97 |

  **The real cause is `-o` + `-i` + `-F` TOGETHER in GNU grep 3.0 (this Git Bash build).** Nothing to do with size, line length or binary detection — it **reproduces on a 23-byte file**: `printf 'artikel artikel ARTIKEL' > f; grep -oiF artikel f` returns **0** where `grep -oi` returns **3**. **This is the Sprint 02 shape the persona already tabulates — *a precise wrong finding gets built on, a vague one gets challenged* — committed by the CEO inside the very correction it was writing.** It would have taught the whole company to reach for `-a` against a problem `-a` does not solve. **The lesson that survives is second-order and it is the one worth keeping: a fix is not verified until it is run against the failing case, and "I understand the cause" is not that test.** The rule now has an executable form — `scripts/measure/count-in-html.sh`, which refuses the flag combination, escapes the pattern instead, prints the ZERO warning inline, and carries both the wrong and the right diagnosis in its header.

183. **FOURTH CONSECUTIVE SPRINT IN WHICH THE CEO FLAGGED AND THE OWNER KEPT EVERYTHING — decisions 62, 93, 160 and now 175.** Twelve points offered across four sprints; twelve kept. **The two-way correction stands and neither half is dropped:** the CEO is systematically over-trimming, because a body-of-work sprint has no time pressure and tidiness is not a reason to cut — *and* the flagging must not stop, because the owner's reasoning has added something the CEO could not reach from the repo every single time. **This meeting it was the permissions answer**, which turned RIGHTS-02 from documentation supporting a negotiation into a record of rights already held. The section is a confidence map, not a budget exercise.

## 2026-08-31 — SEO-11, the SERP-shape census

169. **⚠ CORRECTION TO DECISION 156 — THE SPLIT IS REAL; THE NAMED CAUSE IS NOT ESTABLISHED. GATE ON INTENT, WHICH IS STABLE, NOT ON THE AI OVERVIEW, WHICH THIS DATA CANNOT ADJUDICATE.** Decision 156 measured three queries and concluded the AI Overview causes the CTR collapse. SEO-11 measured **84** (GSC `https://hellokahwin.com/`, 2026-08-01..28, `dataState=final`; Ahrefs `serp-overview` country `my`). **What is established:** at matched positions 3–11, **document-intent queries 10 clicks / 220 impressions = 4.55% (mean position 7.07) against number- and definition-intent 3 / 806 = 0.37% (mean position 7.64) — 12.2x, Fisher exact two-sided p = 0.00002**, and it holds at **p = 0.000003 to 0.000025 under three different re-cuts** of the data. **What is NOT established, in either direction: the AI Overview.** The same test reads **p = 0.102 across all 30 band rows and p = 0.0009 across the 23 whose Ahrefs SERP snapshot is actually from the measurement month** — and that subset is not neutral, because it drops `mas kahwin terengganu` (5.17%) and `idea goodies kahwin` (10.53%), the two best-converting AI-Overview'd queries we own, carrying 5 of the AIO arm's 7 clicks. **A re-cut correlated with the outcome cannot arbitrate the outcome. With 14 clicks in the band the question is undecidable, and the 15x figure is neither confirmed nor refuted.** (An earlier draft of this entry claimed refutation; it was wrong, and the stale-snapshot check that corrected it was triggered by verifying an unrelated sentence.) **Why decision 156 saw the AI Overview regardless: the feature does not sort the classes.** 94% of number-intent queries carry an AI Overview — exactly one in 84 does not — but so do **79% of document-intent queries**, so sorting three queries on the feature also sorts them on intent, while the feature itself separates nothing. Two facts survive the staleness problem, because a `true` reading on an old snapshot is the reliable direction: `mas kahwin kelantan` and `mas kahwin terengganu` are **the same page**, both AI Overview at position 1, positions 6.69 and 6.19 — one earns 0 clicks from 75 impressions, the other 3 from 58; and **`idea goodies kahwin` — decision 156's own healthy-CTR counter-example — carries an AI Overview at position 1** and is the best-converting query on the site (10.53% CTR, ratio 5.68), its SERP never having been checked when 156 was written. **Consequence:** PRE-FLIGHT #1 in `head-of-seo-content.md` selects targets on a variable that fails a robustness check and must be replaced (patch text at `docs/plans/aug-30-2026-session-01/aug-31-2026-patch-preflight-1.md`; the buddy checkout owns the edit); **SEO-12 must gate on `intent_class` + volume** — which is also cheaper and more robust, since intent is derived from the query string and does not depend on Ahrefs having crawled anything. Keep recording the AI Overview; at Sprint 05's click volume it may become decidable. Evidence: `docs/work-done/aug-30-2026-session-01/serp-shape-census.csv`, rebuildable with `scripts/seo/serp-shape-census.py`.

170. **THE PAGE-WORTHINESS THRESHOLD, in Ahrefs `volume` country `my`: number/definition intent needs 2,700 monthly searches; document intent needs 220.** Both derive from a bar of 10 clicks/month divided by the CTR each class achieved at positions 3–11 (10/0.0037 and 10/0.0455). The bar is 10 because the site earned **82 clicks in the 28 days to 28 Aug**; a page that cannot plausibly add 10 is adding under 12% to total output. **Applied to the census this kills new pages for the entire `mas kahwin` state series** — Johor 1,000, Kelantan 900, Perak 800, Negeri Sembilan 700, Sabah 350, Sarawak 350 — all number intent, all below 2,700, together returning **4 clicks from 857 impressions**. The existing pages stay; they hold the cluster's coverage and cost nothing.

171. **DECISION 159's SPLIT CTR METRIC IS NOW A SCRIPT COLUMN, not a per-query judgement.** `intent_class` in `scripts/seo/serp-shape-census.py` is assigned from the grammar of the query itself. Census totals, 2026-08-01..28: **document 4.66% (18 queries, 236 impressions, 11 clicks)**, number 0.49% (38, 815, 4), definition 0.00% (2, 103, 0), navigational 0.00% (7, 352, 0), other 0.13% (19, 752, 1). Census total 0.71%; **ex-quarantine 1.01%** (garden-wedding removed, decision 148). Board reports carry the document and number/definition lines. **Caveat that must travel with the figure: 11 clicks carry the document arm**, 6 of them from one query, and the confidence intervals bound the true ratio only at **no less than 2.3x**. Re-run at the end of Sprint 05 before treating 12x as a constant.

172. **⚠ `gsc-ctr-by-position` (Ahrefs) IS NOT A CTR CURVE — it is an unweighted mean of per-keyword CTRs.** On TheWeddingNotebook, same 28-day window, it reads **17.0% at position 10, 26.2% at 15, 34.9% at 18** — CTR rising with depth, because one keyword with 1 impression and 1 click reads 100% wherever samples are thin. `gsc-keywords` additionally **caps at 250 rows** (`limit=10000` still returns 250), so a curve hand-built from it is truncation-biased at depth. And **our own GSC cannot produce a curve at all**: over an 11-month hold-out (2025-09-01..2026-07-31) the site earned 388 clicks, only **34** attributable to named queries. SEO-11 therefore used a cited external curve (First Page Sage, positions 1–10) and wrote **NA beyond position 10 rather than extrapolating**. **Plot any curve before quoting it; one that is not monotonic through the top ten is an artefact.**

173. **AHREFS HAS NO SERP SNAPSHOT FOR YEAR-STAMPED QUERIES, AND AN EMPTY RESPONSE MUST NEVER BE READ AS "NO FEATURE".** 35 of the census's 84 queries returned zero position rows, including 4 of the 26 at ≥20 impressions — `garden wedding kuala lumpur` (108 impressions), `mas kahwin pahang 2026`, `mas kahwin perak 2026`, `mas kahwin johor 2026`. Verified rather than assumed: re-requested individually, still empty, while `keywords-explorer-overview` returns **volume 150–200 with `difficulty: null` and `serp_features: []`** — Ahrefs knows the keyword and has never crawled the SERP. Almost every uncrawled query is **year-stamped**. Those rows are written `unknown`, never `false`, and excluded from every rate. **Any SERP-feature gate SEO-12 builds needs a defined behaviour for this case or it will silently score a fifth of the corpus as feature-free.**

174. **⚠ AHREFS SERP SNAPSHOTS GO STALE SILENTLY, AND `serp_update_date` IS THE ONLY THING THAT SAYS SO.** The 49 crawled SERPs in SEO-11's census span **2023-08-09 to 2026-08-28**: 37 from August 2026, **11 from July 2026, and one from August 2023** — three years old, predating AI Overviews entirely. Nothing in the response marks a snapshot as old; it looks identical to a fresh one. **This is not cosmetic. It moved SEO-11's AI-Overview p-value from 0.102 to 0.0009** depending on whether stale rows were included, which is what made that finding undecidable (decision 169). **Rules that follow: (a) read `serp_update_date` on every row and report the range, never a remembered summary of it; (b) a `true` feature reading on an old snapshot is reliable — a feature seen in July is very likely still there — while a `false` is NOT, because an absence may since have been filled, so treat stale negatives as unknown; (c) any finding from a SERP-feature census ships with its value under at least two re-cuts of the data.** At the click volumes this company currently has, one subsetting decision moves p by two orders of magnitude, so a single p-value is not a result.

## 2026-08-30 — Sprint 04 planning: the queries Google answers itself

156. **⚠ THE CTR COLLAPSE IS CAUSED BY SERP SHAPE — SPECIFICALLY THE AI OVERVIEW — NOT BY OUR TITLES, NOT BY POSITION, AND NOT BY AN AVERAGING ARTEFACT.** GSC 28d to 30 Aug: clicks **51 → 99**, impressions **2,869 → 6,693**, position **17.7 → 12.4**, but **CTR 1.78% → 1.48%**. Decision 96 had noted three days earlier that CTR was rising *while* position improved, "so this is not volume dilution". It is now. **Per-query, which rules out the averaging objection Sprint 02's retro raised** — these are single queries, so no average describes them: `mas kahwin johor` **156 impressions, position 6.3, 0.64% CTR** where its position predicts 5–7%; `mas kahwin kelantan` 125 / 6.9 / 0.80%; `walimatul urus` 121 / 10.0 / 0.83%. Against `doa pengantin baru rumi` **84 / 3.7 / 9.52%** and `idea goodies kahwin` 19 / 9.2 / 10.53%. **The site converts fine on some queries and at a tenth of expectation on others, and position does not explain the split.** Three `serp-overview` pulls (Ahrefs, country `my`, 30 Aug) found the discriminator, and the third was run specifically to try to break the pattern the first two suggested:

  | Query | Our CTR | Image pack | **AI Overview** | PAA |
  |---|---|---|---|---|
  | `doa pengantin baru rumi` | **9.52%** | Yes (18 results) | **No** | No |
  | `mas kahwin johor` | 0.64% | Yes (9 results) | **Yes — pos 3** | Yes — pos 4 |
  | `walimatul urus` | 0.83% | Yes | **Yes — pos 1** | Yes — pos 5 |

  **All three carry an image pack, so the image pack is controlled for.** The AI Overview is the variable, and it tracks intent: a query wanting a **number** (RM22.50) or a **definition** is answered above us — on `walimatul urus` the AI Overview sits at position **1**, with Dewan Bahasa dan Pustaka at organic 2 and Wikipedia at 6. A query wanting a **document the reader takes away** — the full text of a prayer, to recite at a ceremony — cannot be, carries no AI Overview, and earns a normal CTR. **This is the mechanism behind decision 67**, recorded 24 Aug as an unexplained observation: *"we have proven we can get indexed and have not proven we can earn a click."* Six days later it can be stated per query class. **Consequence: impression growth in the number/definition cluster will not reach decision 7's 1,500-click target, however well those pages rank.** One corollary named but NOT acted on: our own titles hand the answer over (`Mas kahwin Johor 2026: RM22.50 dan asal usul angkanya`). Where an AI Overview has already answered that is not the cause of the loss, and a sitewide title rewrite on a three-query theory is explicitly out of scope — SEO-11 is the test.

157. **🔴 THE CACHED-METADATA / WRONG-`<title>` DEFECT IS CLOSED.** Open and unowned in `ceo-memory.md` since 26 Aug, and carried forward by Sprint 03's retro as still having no owner. **Disproved on 30 Aug by six live fetches, five of them cold** (`X-Vercel-Cache: MISS`, `Age: 0`) — the exact condition CONT-05 and CONT-07 established as the trigger — every one returning its correct title. **SEO-07's `generateMetadata` fix holds under the condition that used to break it.** This was checked FIRST because it was the obvious cause of a CTR collapse, and disproving it is what forced decision 156's real diagnosis.

158. **DECISION 96'S PREDICTION IS SCORED AND IT WAS WRONG, IN OUR FAVOUR.** It predicted the ~22 Sept checkpoint of 150 clicks/28d would be **MISSED** from a base of 51. GSC 30 Aug: **99 clicks**, with daily clicks 5 → 12 → 14 → 15 → **18** across 25–29 Aug and daily impressions reaching **1,413**. Recorded as a scored prediction rather than allowed to expire quietly.

159. **⚠ OWNER DECISION — THE CTR METRIC IS SPLIT.** From Sprint 04 on, board reports carry CTR **separately for document-intent and number/definition queries**. Sitewide CTR stops being a headline figure: a single number that averages a 9.52% query and a 0.64% query **recommends nothing**, and it hid decision 156's problem for a week while the CEO reported it as a success. Clicks/28d remains the north star. SEO-11's census populates the split, which is what makes the classification reproducible rather than a per-query judgement call by the CEO.

160. **OWNER DECISION — Sprint 04 scope is 43 points across 11 items, *Earn the click*, and NOTHING IS CUT.** That is the **third consecutive sprint** in which the CEO proposed cuts and the owner declined every one (decisions 62, 93, and this). Nine points offered, nine points kept. **Recorded as a pattern with a two-way correction:** the CEO is systematically over-trimming, because a body-of-work sprint has no time pressure and tidiness is not a reason to cut — *and* the flagging itself is working and must not stop, because each time the owner's reasoning added something the CEO did not have. See decision 161 for this meeting's example.

161. **⚠ OWNER DECISION — RIGHTS-02 RE-SCOPED FROM CLEARANCE TO LICENSING.** *"I will obtain permissions to use it, just take it, photographers have good relationships with us."* The `jangan-guna` legacy assets are **not stripped**; the owner will license them. **This is strictly better than deletion** — it keeps images on pages that are ranking, and it draws on an asset the CEO could not have found in the repo. **The item flips from removal to enumeration:** its deliverable becomes a census across all 86 article URLs **grouped by photographer or source**, because that is the unit the owner negotiates in — a flat list of 300 filenames is not actionable. Nothing is deleted and **nobody is contacted by us**: approaching photographers is an outward-facing commitment in the company's name, carve-out 3, and the owner has taken it. ⚠ **Recorded rather than re-litigated:** until permission is obtained these images remain live without documented rights. The owner made that call explicitly and named the mitigation. **Follow-up owner: the CEO, at Sprint 05 planning** — because decision 50 is the standing proof that an assignment nobody audits is a note.

162. **CONT-13 CARRIES A SECOND, NON-NEGOTIABLE GATE: RELIGIOUS TEXT ACCURACY.** Doa is simultaneously the highest-converting family the site has (`doa pengantin baru rumi`, 9.52%) and the highest-risk content the company has ever published. **A misquoted prayer is not the same class of error as a wrong venue price** — it is brand-ending with a Malay Muslim audience and it is the kind of mistake that gets screenshotted. Every doa, Arabic string, transliteration and religious claim goes through `editorial-verification-lead` **against a named published authority** (JAKIM, a state mufti's office, DBP, or a recognised published collection), with the authority and date checked recorded **per item**. **If a text cannot be sourced to a named authority it does not ship** — the article publishes without it or does not publish. Same shape as SEO-04's sourcing gate, which correctly killed that item twice. A parked doa article costs 2 points; a wrong one costs the audience.

163. **⚠ THE CARRIED-FORWARD CREDIT FIGURE WAS WRONG, AND THE CEO'S FIRST CHECK WAS WRONG TOO — the second is the more useful finding.** Sprint 03's retro carried *"27 of 48 images on garden-wedding carry NO CREDIT"*. The CEO grepped the live page for `Kredit` and got **zero**, which would have read as **worse** than reported. **The check was wrong: the credits exist under an ENGLISH label.** Enumerating the real casings on that one page — `Source:` ×22, `source:` ×6, **`sOURCE:` ×6**, `SOURCE:` ×6, **40 credits across 49 photos**. So roughly **9 uncredited, not 27**. Two defects nobody had recorded fell out of the corrected check: the credit label is **English on a Malay reader page** — the same shape as the `## SOURCE NOTES` block in the DoD standard's failure mode 5, English scaffolding surviving a conversion onto a page readers see, here on the page drawing **28% of all site impressions** — and **`sOURCE:` renders live, six times**. Confirmed on 3 of 30 further articles sampled (`dewan-kahwin` 14 labels across three casings, `hantaran-tunang` 12, `pelamin-kahwin-dewan` 8). **This is the tenth instance of the tabulated verification shape, and the FIRST caught by the persona's own rule** — *when a check returns a surprising absence, verify the check first* — applied in the same tool call rather than after the fact.

164. **⚠ `sprint-NN.json` — THE FILE THE SKILL CALLS "THE CONTRACT" — IS ABANDONED MID-SPRINT, AND HAS BEEN FOR THREE SPRINTS.** Found during this meeting's planning. `sprint-03.json` reads `state: in_progress` with **24 of 26 items `todo`**; the tracker reads `done`, **107/115**. `/endsprint` writes a separate `-retro.json` and never reconciles the contract file — same for sprints 01 and 02. **A reader opening `sprint-03.json` would conclude nothing shipped.** PLAT-15 fixes `/endsprint` and back-fills 01–03. **The failure mode that hid it for three sprints is that header totals can agree while items disagree**, so the DoD requires an item-for-item comparison, not a header check.

165. **SEO-04 STAYS PARKED AND IS NOT REVIVED.** It parked twice at the same gate, and Sprint 03's retro found the cause neither park had named: `nikahsatu.com` is the venue **operator**, not a competitor we can out-build. The standing rule from that retro is that an item handed back after a park must show **what changed in the DIAGNOSIS, not the method** — and nothing has. Reviving it by changing the method a third time is explicitly forbidden in Sprint 04's `out_of_scope`.

166. **THE CEO WITHDREW ITS OWN PROPOSED CUT OF PLAT-16 BEFORE THE OWNER RULED.** The name collision between the two `creative-director` personas has a zero-cost workaround — always dispatch with the full path — which is exactly why it was proposed for cutting. **Sprint 03's central finding is that prose rules do not fire.** "Remember to type the full path" is a prose rule. Two points to make the trap structurally impossible beats a rule the CEO has to remember, and the CEO reversed itself on those grounds rather than on the owner's.

167. **⚠ CEO ERROR, FOUND AT THE END OF THE MEETING: RIGHTS-02 WAS SCOPED TO PRODUCE A WORKLIST THAT ALREADY EXISTED, AND HAD BEEN BLOCKED FOR FIVE DAYS ON EXACTLY THE DECISION THE OWNER TOOK TODAY.** `docs/plans/aug-23-2026-session-01/aug-25-2026-rights-risk-and-request-list.md` — **381 lines**, written by `managing-editor` on 25 Aug, status **BLOCKED — owner decision on outbound contact**. It already holds: **the real count, 281 `jangan-guna`** (269 `IN-` plus 12 others — so even the 307 figure this meeting called stale was itself superseded five days ago); **ten Real Wedding studios named**, each already credited by studio name in the post bodies (`Jurugambar: Ameir Fikri`, `Jurugambar: Whitenery.co`); a **ranked rights-risk list** (Getty/iStock 1 file, a press photograph 1 file, 401 Real Wedding photographs across 14 live articles, 269 `IN-` vendor images, 64 orphans); **two ready email templates**, BM primary and EN; and **§3.4, four decisions put to the owner**. Its own recommendation matches the owner's instinct almost word for word: *"Exposure is low and we should not spend money on it… the 269 vendor images stay and are left alone. Nothing here justifies a clearance budget, a rights coordinator, or a lawyer."* **The error: the backlog was built from the sprint files, the retro and `ceo-memory.md`, and missed a finished plan sitting in `docs/plans/`. Decision 94 recorded this exact lesson three days ago — for `ceo-memory.md` — and it was not generalised.** **A PLAN MARKED BLOCKED IS FINISHED WORK WAITING ON A DECISION**, which makes the blocked rows of `docs/plans/README.md` the highest-value thing to read at planning time. It was found only because the CEO went to update that index at the very end of the meeting. **RIGHTS-02 keeps its 5 points and is not cut — the work changes rather than shrinks:** refresh the census, take the two institutional deletes, run the free attachment-page check, and hand the owner the populated letters. ⚠ **And a distinction the owner's decision does NOT cover: the Getty/iStock image and the press photograph belong to INSTITUTIONS, not to photographers who know us.** "Photographers have good relationships with us" is not an argument that reaches Getty. Those two come down, with UNDO pushed first.

168. **CEO DECISION TAKEN RATHER THAN LEFT SITTING: `aug-25-2026-enforcing-credit-everywhere.md` is NOT this sprint.** It has sat **BLOCKED — CEO decision on the four to five days of engineering** for five days, in an index nobody re-read. **That is my decision and leaving it unmade was the failure.** RIGHTS-01 fixes the actual observed defect — one Malay label, one casing, the live `sOURCE:` typo — for **3 points**. A four-to-five-day programme to enforce credit on every ingest path is a **Sprint 05 question, once RIGHTS-02 has told us how many assets genuinely need it**. Recorded so it stops reading in the index as though someone else owes an answer.

## 2026-08-29 — SEO-04: parked a second time, same cause as the first

155. **⚠ SEO-04 re-dispatched 29 Aug on a method fix (phone-verify → published
records) that did not address decision 83's actual blocker, and was parked
again for it.** The 27 Aug ownership finding (ceo-memory.md: nikahsatu.com is
Zest Venture Sdn Bhd's own site, not independent coverage of Arjuna, Villa
Rimba Flora, JIWA Damansara or Rumah Abang Jamil) was never about HOW we
verify a price — it was about WHO publishes it. Fixing "an agent cannot make
phone calls" does not fix "the only publisher is the venue's own owner."
`head-of-seo-content` re-ran the check live on 29 Aug — fetched each brand's
own domain/social bio directly, not just nikahsatu's listing — and got the
same answer: all four route booking to nikahsatu's own WhatsApp/rebate funnel,
`villarimbaflora.com` still carries zero RM and zero pax strings, and
nikahsatu's own footer reads "© 2019 NikahSatu by Zest Venture Sdn Bhd." No
pages built, per the brief's own gate; Setiawangsa was correctly NOT built
solo, since running the control without the commercial set it's meant to be
compared against would have been the forbidden quiet downgrade to council
halls. New, and worth keeping separately from decision 83: the real
Setiawangsa control number is **~1,382 impressions over 90 days**, not the
~150 the brief carried forward — GSC `mcp__gsc__get_advanced_search_analytics`,
pulled 29 Aug 2026. Evidence:
`docs/work-done/aug-28-2026-session-01/aug-28-2026-done-seo-04-parked-second-time-same-cause.md`.
Standing rule added to `head-of-seo-content` and `ceo-memory.md`: **a brief
that changes one stated blocker on a parked item must say whether it also
revisits the item's OTHER blockers** — fixing one does not reopen the rest.

## 2026-08-29 — DES-05: the design system, and where the reference page could and could not go

154. **The `/design-system` reference page stays under `/admin/`, not at the
bare path the owner-decision text names, because `src/middleware.ts` scopes
Clerk to `/admin(.*)`, `/login(.*)`, `/no-access` and `/api/v1/inspire(.*)`
only.** The file's own comment states why: Clerk runs "ONLY on admin
surfaces" so the public audience pays no auth latency. A page outside those
prefixes never gets wrapped by `clerkMiddleware()`, so `requireAdmin()`'s
`auth()`/`currentUser()` calls would either throw or resolve to a
signed-out state some code path could treat permissively — the opposite of
"private." Basis: read the middleware file before moving the route, not
after. Consequence: `/admin/design-system` is the durable location for this
surface, and any future item wanting the bare `/design-system` path needs to
widen the Clerk matcher first, deliberately, as its own change — not as a
side effect of a design-system item. Full record:
`docs/work-done/aug-28-2026-session-01/aug-28-2026-done-des-05-design-system.md`.

## 2026-08-29 — CONT-11: the four zero-volume C2.1 head terms, settled

153. **⚠ CONT-11 — four C2.1 head terms carrying ZERO `volume` and no `parent_topic`
are DECIDED: one replaced, three dropped, none invented.** Ahrefs Keywords Explorer
`volume` (12-month average), country `my`, 29 Aug 2026. **`adat hantaran` →
`adat hantaran kahwin`** (`volume` 20, `volume_monthly` 22, `parent_topic`
`balas hantaran`, `traffic_potential` 40 — small, real, and not owned by any C2.1
sibling). **SERP-ownership run against the replacement itself**, not only against
what was rejected (Sprint 02's retro rule): organic position 1 on both
`adat hantaran kahwin` and its parent `balas hantaran` is the same page,
`locco.com.my`, DR 25, one 2020 blog post, 56 estimated monthly traffic — no
official portal, no entrenched authority, and position 3 on the parent is
ppsignature.com (DR 4, decision 13's hidden competitor) earning 396 traffic from
one URL. **Expected position stated honestly at 3–5, never 1**, per the standing
rule against promising the top spot on a brand-new target. Live edit shipped on
`adat-hantaran-ikut-keluarga`: `title` 53→60 chars, `meta_description` 148→155
chars, both carrying the new phrase, both passed `/humanizer` before the write,
undo committed and pushed BEFORE the UPDATE ran, verified live after the Vercel
edge TTL (no `VERCEL_TOKEN` available, same condition CONT-12 hit — the SECOND
request past the 300s TTL is the one quoted, per Stage 7). **`hantaran kahwin
bajet` and `kos hantaran kahwin`** (both read as the same topic-4 budget article,
`hantaran-kahwin-bajet` — no surviving brief table names which term belonged to
which article, so this is stated as an inference from live content, not a
certainty) and **`persiapan hantaran`** (`persiapan-hantaran-kahwin`) are
**DROPPED**: confirmed `volume` 0 AND zero rows from `serp-overview` — a stronger
absence than a rounded-down zero, meaning Ahrefs holds no record of these exact
strings at all. Two matching-terms sweeps (top 200 by volume on seed `hantaran`;
a 250-row substring sweep across `bajet`/`kos`/`murah`/`jimat`/`adat`/`persiapan`/
`persediaan`/`senarai`/`harga`) surfaced only candidates that either shared a
parent topic already owned by a sibling article in the same cluster (rule 4) or
did not match the article's actual content (`checklist kahwin` at 800/mo is a
whole-wedding checklist, not the hantaran-only 8-week schedule the live page
covers). Both articles stay live and unchanged; their organic value is cluster
completeness and internal-linking equity, not a ranking play on their own phrase.
Evidence: `docs/work-done/aug-28-2026-session-01/aug-28-2026-done-cont-11-target-terms.md`.

## 2026-08-27 — Sprint 03 planning: convert the impressions we now have

89. **Decision 50 is CLOSED, not re-assigned — `dewan komuniti setiawangsa` is dead as a target.** GSC 28d to 27 Aug: **100 impressions, position 9.0, ZERO clicks**, against 25 impressions at 8.9 when it was assigned on 24 Aug. Four-fold impression growth, three days holding page one, no click ever. Decision 82 had already killed it on significance grounds and decision 83 killed the category structurally (~30 searches/mo; DBKL's own DR 64 booking portal at positions 1–2). **What the audit added: decision 83 declared Setiawangsa a deliberate CONTROL so the council-hall category would be proved dead rather than quietly dropped — and SEO-04 parked before reaching it, so the control never ran.** Folded into SEO-04's revival at no extra cost. The audit's job was to score an assignment; it found a second, quieter thing that had evaporated inside the first.
90. **⚠ SEO-07 LEADS SPRINT 03, AHEAD OF ALL NEW CONTENT — because we cannot currently measure our own titles.** The `mas kahwin` family draws ~300 impressions at positions 6.5–12.2 and earns **3 clicks**; four of its five pages earn zero. SEO-05 repaired exactly these titles in Sprint 02. **We cannot establish whether the repaired titles are what Google is served**, because `generateMetadata` caches its own fallback and a verified repair can render as the site-default title 14 minutes later. Every title decision downstream is unmeasurable until this is fixed. Writing more pages that rank at 8 and earn nothing compounds the wrong thing.
91. **Ahrefs is DEMOTED to competitor comparison only for this site.** Ahrefs MY, 27 Aug: **9 organic keywords, ~31 organic traffic**. GSC the same day: **2,869 impressions, 51 clicks**. Ahrefs lags by roughly two orders of magnitude on a site this small. It stays the tool for SERP ownership and competitor volume — decisions 13 and 83 both rest on it — but quoting it as a *performance* metric would understate the company badly and has no place in a board report.
92. **SEO-04 REVIVED via phone verification, gate retained verbatim.** Its own park recommendation is the way back in: phone-verify eight venues, publish with "disemak Ogos 2026" — the method that built `mas-kahwin-ikut-negeri` and the method the company's whole competitive claim rests on. **The gate is unchanged and still allowed to kill the item:** if eight venues cannot be verified it STOPS, does not invent a capacity, and does not downgrade to the council-hall target decision 83 already killed. Sprint 02's retro established the gate firing as a deliverable rather than a failure.
93. **Sprint 03 scope set at 52 points across 13 items** — *Convert the impressions we now have*. **The CEO's own cut list (PLAT-10 and PLAT-12, 4 points) was declined by the owner and both stay in scope** — the second sprint running in which the owner has taken the CEO's proposed cuts back off the table (decision 62 was the first).
94. **Two open items were added DURING planning, from `ceo-memory.md`, that the CEO's first proposal missed** — SEO-10 (**31 articles emit no FAQ schema**, on a site whose entire Malay long tail is question-shaped; owner role was retired to BMAD, which is why it sat unowned) and CONT-10 (**the C2.1 merge decision**, deliberately undecided pending a SERP check never run). **The absence is the finding:** the proposal was built from the sprint files and the decision log, and `ceo-memory.md` carries open items that neither of those records. A planning pass that reads only the sprint artefacts will keep missing them.
95. **⚠ THE DELEGATION LAYER FAILED COMPLETELY AND THE MEETING RAN WITHOUT IT.** Six sessions were launched to fill the `ceo-hellokahwin` seat and all six failed between 07:02 and 07:54 on 27 Aug — **four died on `Login expired · Please run /login`** (`ceo-hellokahwin`, `ceo-board-03`, `hk-audit`, `hk-backlog`) and **two went silently idle returning no output at all** (`ceo-hk`, `hk-data`). The window between launch and death shrank from **~25 minutes to ~5**. The meeting was run directly in the orchestrator session, whose credentials stayed valid throughout; every figure in the minutes is a first-hand pull. **`resume-fleet.sh` — built in Sprint 02 for precisely this failure — did not fire.** That makes this the SIXTH instance of the retro's sharpest finding: a rule that was correct, committed and deployed, and did not reach the moment it was written for. Escalated to the owner as carve-out (1); **Sprint 03 should not start until it is resolved**, because `/startsprint` cannot dispatch into a fleet that dies on launch.
96. **Performance snapshot, recorded so later meetings can score it.** GSC 28d to 27 Aug: **51 clicks, 2,869 impressions, CTR 1.78%, average position 17.7.** Sitemap **Valid, 103 indexed URLs, 0 errors**, last fetched 26 Aug 22:03 — against decision 63's 78-URL sitemap last fetched at 73 URLs. **RISK-04 is confirmed to have worked.** The daily trend is the sprint's real result: impressions 148 → 252 → **412** across 24–26 Aug, with 26 Aug's average position at **10.0**. Sitewide CTR moved 1.65% → 1.78% *while* position improved 20 → 17.7, so this is not volume dilution. **Against decision 7's target — 1,500 clicks/28d by 21 Nov, 150 at the ~22 Sept checkpoint — the company stands at 51 and needs 3× within four weeks. The first checkpoint is predicted MISSED, stated now rather than explained afterwards.**
97. **`garden-wedding` answered with the data decision 88 deferred it for: 814 impressions, 4 clicks, CTR 0.49%, position 36.6.** That is **28% of all site impressions earning 8% of clicks**, from an English-intent page on a Malay-first site sitting on page four — the largest single distortion in every report the company produces. SEO-08 owns the decision (rewrite Malay-first / keep and stop reporting it / drop it) and must run the SERP-ownership rule against whatever *replaces* it, not only against the page it kills — the exact failure Sprint 02's retro caught.

98. **⚠ CORRECTION TO DECISION 95 — TWO FAILURES WERE CONFLATED, AND THE OWNER WAS SENT AFTER THE WRONG ONE.** Decision 95 reported six delegate failures as one problem and named the login expiry as the blocker, making `/login` the meeting's single owner request. **A probe at 17:48 on 27 Aug separates them.** A deliberately trivial two-command agent (`curl` a status code, `git rev-parse`) went idle `available` returning NOTHING, twice, and did not answer a direct message either — **and reported no auth error at all.** A two-command task cannot run long enough to hit a token refresh, so this failure is not authentication.

  | | Symptom | Observed | Status |
  |---|---|---|---|
  | **A** | `Login expired · Please run /login` | 07:02–07:54 only | **Unproven now** — may have cleared on its own |
  | **B** | Idle `available`, no output returned, unreachable by message | 07:22, 07:52, **17:48 ×2** | **Reproducible right now** |

  **B spans both windows and is almost certainly independent of A.** The CEO folded B into A because four of six failures carried an auth banner and the two silent ones were assumed to be the same cause wearing a quieter face. **That is this company's named failure shape — unknown collapsing into the value that means ‘fine’ — committed by the CEO inside the very meeting that recorded it as the shape.** The owner request in the 27 Aug minutes stands corrected: **`/login` is not established as the fix, and running it may change nothing.**

99. **The scope of B is NOT yet established, and the sprint's blocked status is therefore UNKNOWN rather than confirmed.** Every failure so far — all six delegates and the probe — was an **in-process subagent**. `/startsprint` does not use that path: `dispatch-agent.ps1` opens an Orca terminal running a **fresh `claude` CLI session in its own process with its own credentials**, which is a different mechanism. Sprint 02's retro states plainly that a `/login` in the orchestrator does not reach dispatched agents because each holds its own credentials — the same separation that makes the dispatch path possibly healthy while the subagent path is broken. **Not asserted, because it has not been tested.** The next action is to dispatch one throwaway agent through `dispatch-agent.ps1` and watch it, rather than to declare the sprint blocked or unblocked from subagent evidence that may not apply to it.

## 2026-08-28 — Sprint 03 execution: the opsz call, and four agents correcting the CEO

143. **⚠ CEO DECISION, owner delegated: re-cut all five lockups at `opsz 6` BEFORE merging `/brand`, rather than merging as-is and correcting after.** DES-13 found that Bodoni Moda's `opsz` axis **barely touches the stems** — the H stem moves 181 → 179 units across the entire 6..96 range, 1.1% — **but nearly erases the hairline.** It is a contrast axis wearing a size label. Every lockup is used SMALL (an 18px header, a 16px favicon), and at the font's default `opsz 11` the hairline thins at that size; at `opsz 6` the 18px mark composites to `#33302C`, **10.91:1**.

  **The decisive reason was not typography, it was distribution.** `/brand` is public and its marks are DOWNLOADABLE. Merging as-is puts the wrong cut in people's hands, and a later correction strands them — the exact failure DES-13 was sequenced ahead of DES-11/DES-12 to prevent. The face did not change but the INSTANCE did, which is the same failure mode wearing a smaller hat. And the page **asserts "Min height 18px"**: at `opsz 11` that assertion was false, so merging as-is would have published a claim the asset could not meet.

  Tracking was **re-solved, not carried over** — `opsz 6` widens the set width of HELLOKAHWIN, so the primary needed 0.092em rather than 0.126em to hold 10.0:1. **The article `<h1>` webfont stays pinned at `opsz 11`** and the registry now says so, because it is a different surface at 30–44px and unifying them would undo this.

144. **⚠ FOUR AGENTS CORRECTED THE CEO IN ONE SPRINT, EACH WITH BETTER EVIDENCE THAN THE BRIEF THEY WERE GIVEN.** (a) **DES-06**: the CEO briefed "the site has no search at all" from decision 76; the agent measured and found search EXISTS but returns zero results for **84.3% of real arrival queries covering 70.9% of a month's impressions** — a sharper and more actionable finding than the premise it replaced. (b) **SEO-10**: the CEO called FAQ schema "the cheapest rich-result opportunity we have"; the agent established that **Google retired the FAQ rich result on 7 May 2026**, and said so against its own item's value rather than shipping a hollow win. (c) **DES-13**: correctly refused to treat the CEO's overstated "Malay diacritic coverage" gate as a blocker. (d) **RISK-07**: refused to mark itself done on three of four DoD clauses and filed an issue for the fourth. **This is now the third consecutive sprint in which the team disproved the CEO more often than the reverse, and every correction arrived inside a deliverable rather than as an argument.**

145. **⚠ THE STATUS BOARD'S DOCUMENTED TELL IS WRONG, AND IT IS WRONG IN THE DANGEROUS DIRECTION.** Sprint 02's retro says a stalled agent reads `IDLE/DONE`. On 28 Aug four agents stalled on `Login expired` and the board showed all four as **`WORKING`**. So neither string is trustworthy: `IDLE/DONE` can mean finished, and `WORKING` can mean frozen. Only reading the terminal distinguishes them. `skillcentral/skills/hellokahwin/scripts/status-board.py` and the `/startsprint` auth section both need this.

146. **`resume-fleet.sh` clears the banner but does NOT always resume the agent.** Of four nudged, **one (RISK-08) restarted itself and three sat at an empty prompt** until sent an explicit continue. The skill claims the interrupt is sufficient — for three of four it was not. That is a script fix, not a memo.

147. **⚠ THE ORCA TERMINAL IS A SCREEN, NOT A LOG — and the CEO mismeasured it FOUR separate ways in one session.** (1) Reading only the visible screen, which missed a brief filename that had scrolled off and produced a false MISMATCH. (2) "Fixing" that with `--cursor 0`, which reads an EARLY slice and reported "NO EXIT LINE" for six agents that had all reported cleanly. (3) Grepping for `ITEM EXIT: 0` unanchored, which matched **the brief's own instruction text** echoed on screen and nearly reported two working agents as finished. (4) Detecting activity by spinner word, against an incomplete word list, which reported six busy agents as idle. **Every one would have produced a false report, and they err in both directions.** The rule: anchor every match (`^\s*ITEM EXIT: [0-9]+\s*$`), and never conclude from the terminal alone — pair it with the status board, the work-done entry, or the git remote.

149. **⚠ DES-02 IS DELIVERED AND DELIBERATELY NOT MARKED DONE — three directions are on one canvas and the owner has not yet named one.** `https://claude.ai/code/artifact/1936d75a-b30a-47ec-9239-fde2c232c0b9`, eleven artboards on four pages: the live `mas-kahwin-ikut-negeri` article drawn three ways at 1440px, each direction at 390px in light and dark side by side, the real 38-article `Hantaran & Mas Kahwin` category, and four stress cases. **A · WARKAH** puts the record above the fold and the photograph inside the grid at column width. **B · DULANG** makes the photograph the page, full bleed, headline on its own parchment ground and never over the picture. **C · MARGIN** runs a 68ch column beside a permanent 268px rail carrying the record's meta. They vary in composition and photographic scale, not in palette — which is exactly what DES-01 section (h) predicted DES-02 would do. Every headline, deck, table row, category and article title is copied from the live site; the longest stress title is the corpus's real 95-character one. The sprint item's state is `blocked` with `awaiting-owner-choice`, because its DoD says the owner names the winner and closing it on the artefact alone would narrow the DoD.

150. **⚠ OUR PHOTO LIBRARY SPLITS FOUR TO SEVEN, AND THAT — NOT TASTE — DECIDES A AGAINST B.** Eleven photographs were pulled from `images.hellokahwin.com` for the canvas, each from the page that uses it. **Four are close-up object frames** (brass tepak sirih on a dulang, a Quran in a white cage, two frames of stacked dulang under red cloth) and hold at any size. **Seven are documentary group snapshots**, several 2009-era Flickr, and they come apart above roughly 300px. Page 3 of the canvas shows it without an argument: B's catalogue runs six covers at 330px, three look expensive and three look like a family album, in the same grid with the same treatment. **B is therefore not a prettier version of A — it is a commissioning decision wearing a layout costume, and the commission is not budgeted.** The creative-director recommends A on that basis. DES-01's photography section knew the cover must depict its subject; nobody had recorded how large the frame is allowed to get.

151. **A TINT OF THE FOUR IS NOT A FIFTH COLOUR — the ONE COLOUR RULE is now obeyable, and it produced the control-boundary token DES-05 needs.** DES-01 set the ceiling at four palette values per page and then listed six tokens, including a raised ground and a hairline; rules and stripes take any real page past four. Every rule, stripe and boundary in the comps is drawn as an alpha tint of the ink or the parchment, so each page renders in exactly four hexes. Measured: ink at 5% → `#E2DFD6` (1.11:1, table stripe), 12% → `#D3D0C8` (1.28:1), 22% → `#BEBBB3` (1.59:1, section rule), and **47% → `#88857E` at 3.06:1**; parchment at 20% on night → `#3F3C37` (1.71:1) and **37% → `#64615B` at 3.05:1**. The last two close the ≥3:1 gap DES-01 flagged for input borders and focus rings. Thread gold `#A8823C` measures 2.95:1 on parchment and still cannot do that job. **Also binding on the DSE: no text is set over a photograph in any direction** — a ratio against a picture cannot be measured, so B overlays its headline on a parchment card and every credit sits on the ground.

152. **⚠ THE SAME SUPERSEDED RULE SURVIVED A SECOND CORRECTION, IN THE EXACT WAY DES-01'S RETROSPECTIVE SAID IT MUST NOT.** DES-01 wrote the rule — *"when a directive is superseded, grep for its old wording and fix every copy in the same change"* — and reported `sprint-03.json` as the only surviving copy. **`.claude/agents/creative-director.md` line 129 still opens its hard rules with "HUMAN PHOTOGRAPHY ONLY".** The grep was scoped to tracked files and that path is untracked by design (decision 20), so a retired instruction fell through the gap between two individually correct decisions. **The rule needs one word: grep the WORKING TREE, not the index** — an agent reads files on disk, and git does not know which of those the next agent will believe. The creative-director did NOT edit it: a persona is that agent's own configuration and a brief cannot authorise rewriting it. **`ceo-hellokahwin` owns the edit**, via skillcentral and `/endsprint`. The stake is concrete: applied literally today, the retired rule would have disqualified the four best photographs we own, because they are objects rather than people.

148. **⚠ SEO-08 — `garden-wedding` is KEPT and QUARANTINED, the rewrite option is dead on the data, and decisions 88 and 97 both described the page wrongly.** It is **not an English page**. Fetched from production at 2026-08-28T07:54:29Z the `<title>` and `<h1>` both read *"20 Venue Garden Wedding Paling Cantik di Malaysia"*, over 2,332 words of Malay and twenty `<h2>` headings that are twenty real Malaysian venues. What is English is the **query**, and `garden wedding` is the loanword Malaysians use — there is no Malay phrase competing with it. **Rewriting it Malay-first would trade 1,350 searches a month for zero.** Ahrefs Keywords Explorer `volume` (the 12-month average), country `my`, 28 Ogos 2026: the loanword family totals **1,350/mo** (`garden wedding` 500 at KD 0, `garden wedding kl` 150, `garden wedding malaysia` 150, `garden wedding kuala lumpur` 150, and seven more); every Malay phrasing reads **0**, and eight of them — `majlis kahwin luar`, `tempat kahwin taman`, `majlis kahwin di taman` among them — return **no row at all**, meaning Ahrefs has no record of the phrase in its Malaysian index. A `matching-terms` sweep over four Malay seeds returned 105 ideas and **every wedding-in-a-garden one reads 0**. **This is the Sprint 02 false pass run the right way round: the replacement was measured before the recommendation was written, and the replacement is what failed** — on the volume half of the rule, one step before anyone looks at who ranks. The wider variant (repoint at Malay venue terms) fails worse: `tempat kahwin` 30, `tempat perkahwinan` 20, `venue kahwin` 20 = ~70/mo, and the only Malay venue term with demand is `dewan kahwin` at 350/mo, which is a different entity already held by our best page at position 9.4.

  **SERP ownership, Ahrefs `serp-overview` country `my`, 28 Ogos 2026 (snapshots 25–26 Aug): nobody owns position 1 the way DBKL owned the council halls — Google does.** On `garden wedding` position 1 is a **local pack** (Dani Hill, Glasshouse at Seputeh, Tepian Rimba Villa) and position 2 an image block. The first organic result is **theweddingnotebook.com at 3, DR 44** — *the company's own English property*, running the twenty-venue listicle ours was translated from. Same page holds **2** on `garden wedding kl` and **4** on `garden wedding malaysia`. hellokahwin.com is **DR 0.0**. So this SERP is already served at group level, and pushing a DR 0 domain at it means competing with ourselves behind seven stronger domains. **Honest expected position after real work: 8–15. Not 3, never 1.**

  **Dropping was rejected because a drop has no destination.** No Malay page covers outdoor or garden venues, so a 301 would be a soft-404; a 410 deletes a working article to fix a reporting problem; `noindex` keeps the maintenance and surrenders a 1,350/mo option at difficulty 0.

  **The reporting consequence is the biggest number in the item, and it ships as a command, not a promise — `scripts/seo/gsc-headline.mjs`.** GSC 28d to 27 Aug, pulled first-hand: site **54 clicks / 3,088 impressions / 1.75% / position 16.72**; **ex-quarantine 51 / 2,310 / 2.21% / position 9.98.** Over decision 97's own window: 1.61% at 20.09 against **2.29% at 10.41**. **One page has been hiding the difference between a site on page two and a site on page one.** Decision 96's "CTR 1.78%, average position 17.7" understates the editorial estate; quote the ex-quarantine line from now on, with the site line beside it so nothing is concealed.

  Also established: the family has **one** live URL string in GSC (the legacy `/garden-wedding/`, 778 impressions, 3 clicks, position 36.73; the canonical earns nothing), **40 named queries and not one in Malay**, and **1 impression total at position ≤ 13** — so a title rewrite here buys nothing. The 0.39% CTR is *in band* for position 37; there is no conversion defect. Decision 97's 814/4/36.6 reconciles: its own window now reads 856/4/36.41 after GSC backfill. **Reviewed 27 November 2026** on a stated tripwire (≥1 query at position ≤ 20 with ≥30 impressions; best today is `garden wedding` at 30.1 on 140). Failure means the page stays and leaves the review rota, not that it is deleted. **Carve-out for the owner: 27 of the 48 images on this page carry no credit at all** and the 21 that do say `SOURCE:`/`sOURCE:`/`source:`/`Source:` on a Malay page. That breaks an owner-level rule, it is a rights obligation rather than an SEO one, and it is explicitly NOT covered by this item's "no editorial investment". Evidence: `docs/work-done/aug-28-2026-session-01/aug-28-2026-done-seo-08-garden-wedding-decision.md`.

## 2026-08-28 — The wordmark is locked, and the thesis bends to it

111. **⚠ OWNER LOCKED THE CARATS & CAKE REGISTER FOR THE WORDMARK — against the CEO's stated objection, which is recorded as overruled rather than repeated.** The objection: a 10:1 outlined wordmark is almost entirely the space between its letters, which is the purest expression of the whitespace-as-luxury encoding that decision 101's whole direction argues is culturally specific and wrong for a Malay audience. **Both cannot be fully true.** The owner chose the mark. **Consequence, stated once and now binding: the density thesis SOFTENS — the register bends around the wordmark rather than the reverse, and DES-01 receives that as a constraint rather than as an open question.**

112. **Their marks were MEASURED, not described — and the sister brand confirms it is a house formula.** Fetched from production SVGs on 28 Aug: **Carats + Cake 1595 × 160 = 10.0:1**, and **Bliss & Bone 612 × 55 = 11.2:1**. Both are outlined paths, pure black, no symbol; the typeface is **`ivyora-display`** via Adobe Typekit. Two brands, one formula. **The name fits without strain** — Carats + Cake is 13 characters, Bliss & Bone 12, HelloKahwin 11 — so this was never a fit question, only a should question.

113. **The mobile cost is real and is now DES-12's problem rather than an unknown.** Bliss & Bone ships in their header at **250 × 22**, which on a 360px phone is **69% of the viewport**, leaving 78px for search and menu — on a site where search is the most-needed header component and **64% of impressions arrive on mobile at position 8.7, against desktop's 28.9**. Carats & Cake can afford that trade; we are importing a decision made for a different device mix. DES-12 must prove the mark and a reachable search fit together at 360px, and a failure there is a finding to bring back, not a licence to breach the mark's stated minimum height.

114. **Five lockups generated as OUTLINED SVG, matching their construction rather than approximating it.** Letterforms converted to paths with fontTools — no `<text>`, no font dependency, largest file 2.8 KB. horizontal **10.0:1** (primary, tracking solved to hit their exact ratio), horizontal-wide 12.4:1, vertical 2.8:1, shortmark 6.8:1, monogram 1.9:1. **Every mark is `fill="currentColor"`, so ONE file serves ink, parchment and gold** — which is why `/brand` ships no reversed variants and demonstrates reversal by changing the ground instead. ThePickleBase needs a PNG per colour per lockup; we need none.

115. **`/brand` built to thepicklebase.com/brand's real structure, read from that repo rather than guessed.** A typed asset registry drives the sections instead of paths hard-coded in JSX. Ours adds **measured contrast ratios** to the colour section and **six named DON'Ts**, each describing a specific way to produce a near-miss of the real mark. Deliberately NOT copied: the Coach/Court/Events sub-brand families — HelloKahwin has one brand, and inventing sub-brands to fill a page template would be designing an org chart we do not have.

116. **⚠ THE SHIPPED WORDMARK IS PROVISIONAL, AND MUST BE DESCRIBED AS SUCH OUTSIDE THE COMPANY.** All five marks are set in **Bodoni Moda as a stand-in** for `ivyora-display`. In a text logo the typeface IS the logo, so DES-13 — licence, cost, coverage — decides whether what ships is final. If a different face is chosen, all five regenerate; they are never rescaled.

117. **⚠ CEO CORRECTION: "Malay diacritic coverage" was written into THREE personas as a hard typeface gate, and it is much weaker than that.** Malay in Rumi script is essentially plain Latin. The real constraints on a display face here are **licence and cost**, not glyph coverage. Recorded because the overstatement was about to block a font decision on a criterion that barely applies — and because a fabricated-sounding gate in a persona is worse than no gate, since the next agent has no way to know it was invented.

118. **C2.1 IS DECIDED — DO NOT MERGE. The cluster stays at EIGHT published articles; no URL changes, no redirects, no migration. CONT-12 plans against 8.** Settled on the SERP, not on `parent_topic`, which is what the item was held open for. Ahrefs `serp-overview`, country `my`, run 28 Aug 2026; volume field **`volume`** (Keywords Explorer 12-month average) throughout. Six SERPs pulled, overlaps computed by script into `docs/work-done/aug-28-2026-session-01/aug-28-2026-cont-10-EVIDENCE/overlap-matrix.txt`. **Every groom-query against every bride-query pair shares exactly 2 organic URLs (Jaccard 18–20%), and those 2 are the only pages appearing on all six SERPs — nikahsatu's and songketdunia's combined "lelaki dan perempuan" articles. Remove them and groom and bride share ZERO results.** Corroborated by a competitor already running the experiment: `story.motherhood.com.my` (DR 48) publishes BOTH angle pages on one parent topic, each at `best_position` **1** on its own angle, **with no keyword in common** (Site Explorer, `my`, 28 Aug). And Google's People-also-ask box on the groom query prints the groom question and the bride question side by side — the same evidence shape that settled C2.2.

119. **⚠ THE THRESHOLD CAME FROM THE CLUSTER, NOT FROM THE INDUSTRY — and that is the transferable part.** The published rule of thumb (top-10 overlap above ~60% means one intent) has never been calibrated on thin Malay long-tail. Two queries in this cluster are *already* targeted by one article, so their own overlap is the "same page" reading for this data: **40% and 83%**. Every pair the merge question was about came in at 15–20%. **Written into the persona as the tie-breaker for the rule 2 / rule 4 conflict, which this item does NOT resolve in general and does not claim to.**

120. **ONE BOUNDARY IS REPORTED OPEN RATHER THAN SMOOTHED, and it produced the more useful finding.** The legacy seed `hantaran-kahwin` against `barang-hantaran-perempuan` reads **Jaccard 40% (25% with the constants removed)** — level with the within-article baseline, and a clean pass under any borrowed 60% rule. Still not a merge: the seed's own SERP is half money (Shopee, *duit hantaran hak siapa*, *wang hantaran*, Loanstreet) and its PAA asks *Apakah maksud hantaran kahwin?* and *Apakah mas kahwin dan hantaran di Kelantan?*, while the live page (fetched 28 Aug, HTTP 200) centres on **"20 Idea Hantaran Kahwin Lelaki & Perempuan"** — the job topics 2 and 3 now do. **Action for CONT-12: re-angle the seed toward definition and money; it changes one body, not the count.** Second half of the same read: publishers have differentiated the GROOM angle and left the BRIDE angle alone. On `hantaran untuk lelaki` (snapshot 18 Aug) **four dedicated groom-only pages sit in the top 10** — motherhood 3, Zalora 6, ecentral 8, thekenduri 10 — plus therichscents and atokpencen deeper on `barang hantaran lelaki`. `barang hantaran perempuan` (5 Aug) returned **six organic results and not one bride-only page**; the single dedicated bride page appears in that SERP's AI Overview citations. **Named rather than resolved in our favour: Site Explorer gives that same bride page `best_position` 1 on the term while the 5 Aug crawl does not show it in organic at all.** Two different measurements, both quoted as read; the decision does not turn on which is currently true. **The bride page is the least contested asset in the cluster; merging it away would have surrendered the one position with no incumbent in it.**

121. **⚠ DECISION 100 IS CORRECTED: the site does NOT run "shadcn defaults on Geist", and the design track was about to be built against the wrong description of what it is replacing.** Measured against `origin/master` at `59a4077` and against the CSS the browser actually downloads on 28 Aug. **Tailwind is right — v4, CSS-first, no `tailwind.config` file has ever existed in this repo.** The rest is not. The token layer is **The Wedding Notebook's ratified "Plum Forward" v2 system** (`globals.css` cites its own provenance, `DESIGN.md` of 2026-07-04), ported wholesale — plum, brass, two parallel radius scales, a z-index scale, a maintained-but-unwired dark palette — across 2,320 lines. And the public site was **already re-skinned on 27 Aug**: UX-03 (`78cd345`) added `.hk-public`, an ink-on-paper monotone with ten editorial primitives and its contrast measured in comments. Recomputed from the served hex values: ink 17.81:1, `--muted-foreground` 6.88:1, `--border-strong` 3.01:1. **"On Geist" is also wrong for readers** — production ships **zero webfont bytes**, 0 `@font-face` rules, 0 `woff2` references; `--font-geist` resolves to a system stack and Geist loads only in `(admin)/layout.tsx`. **The generic look is INHERITED, not default** — which changes the fix from "configure Tailwind properly" to "unpick another brand's system", and those are different items.

122. **STACK DECIDED (DES-04): Tailwind STAYS, Radix STAYS, shadcn-the-dependency GOES. The two questions were answered separately, as the DoD demanded, and they got different answers.** **Tailwind v4 stays** because in v4 the theme *is* CSS — `@theme` inside `globals.css` — so "replace the visual defaults" and "keep the utility engine" are the same edit, and removing it would mean rewriting **5,481 class tokens across 136 files, 4,038 of them (74%) in an admin console nobody asked to redesign**. **Radix stays unconditionally**: 13 of 28 components wrap a real behavioural primitive, and Radix ships no colour, no radius and no font — removing it would cost weeks and change zero pixels. **`shadcn@^3.8.4` and `components.json` go**, with one named precondition: `globals.css` imports `shadcn/tailwind.css` at runtime, and those **1,669 bytes contain nine Radix data-attribute variants and two accordion keyframes — no colour, no radius, no font**. Copy them to `src/design-system/variants.css` first, or every `data-open:` utility in the tree silently stops generating. `components.json` goes because while it exists, `shadcn add` will drop a stock `new-york`/`neutral` component back in. Full argument, costs of all four options, and six named falsifiers: `docs/work-done/aug-28-2026-session-01/aug-28-2026-done-des-04-stack-recommendation.md`.

123. **⚠ THE FALSE PASS THE BRIEF NAMED WAS REAL, AND SO WAS THE OPPOSITE ONE.** "shadcn looks generic, so rip out shadcn" resolves in practice to ripping out `radix-ui`, because that is what `src/components/ui` imports — the named way to throw away accessibility to fix a look. But the *defence* of Radix nearly shipped false too. **A transitive import trace from the ten public entry files says the public site reaches exactly 3 of 28 components — `button`, `chip`, `pagination` — and the only Radix import in any of them is `Slot`, a composition helper with no accessibility behaviour.** The public site ships **no Radix behavioural primitive today**. The real case for keeping it is **DES-06: search is a combobox** (`aria-expanded`, `aria-activedescendant`, arrow-key navigation over a listbox the input keeps focus inside), one of the most-failed patterns on the web, on a site where 64% of impressions arrive on mobile hunting a named hall or a state's rate. Counting beat asserting, in both directions.

124. **Two accessibility findings handed to DES-05 before anything is built on them, plus one duplication caught at 33 minutes old.** (a) **The proposed palette has no 3:1 boundary token.** Every text pairing in `tokens.css` clears AA and its four documented corrections verify to the second decimal (4.55, 4.56, 4.52) — but `--rule` measures **1.52:1** and `--rule-accent` **2.95:1**, so neither can carry the edge of a control under WCAG 1.4.11. The live site already found and solved exactly this once, with `--border-strong` at 3.01:1. (b) **The public site has no dark mode at all** — 0 `prefers-color-scheme` rules in the served CSS, no `.dark` on `<html lang="ms">`, no dark variant of `.hk-public` — while DES-03's DoD requires light AND dark. That is new work in the budget, not a carry-over. (c) `tokens.css` (branch `feat/des-05-design-system-reference`, 10:17) and `brand.css` (branch `feat/des-10-brand-page`, 10:50) hold **the same palette written by hand twice**, the second acknowledging the duplication in its own comment. Both branches also hand-authored plain CSS and bypassed Tailwind entirely — correct for a scoped preview with no decided stack, wrong as an architecture. **With the stack decided, one of the two copies is deleted.** Separately: **DES-05 was built before the gate that blocks it opened.** The work is good and this recommendation lifts it, but a gate that does not hold is not a gate — flagged for the sprint retro.

125. **⚠ REGISTER PROPOSED (DES-01), AND IT CONTRADICTS THE REFLEX THE WHOLE BRIEF POINTED AT: serif display is the WESTERN signal in this market, not the premium one.** Five Malay-audience premium brands fetched 28 Ogos 2026 and measured from their delivered HTML. **Four use geometric or humanist sans** — ADNAA (`adnaa.com.my`, Montserrat/Poppins/Roboto), Rizman Ruzaini (`rizmanruzaini.com`, Montserrat + Felix Titling), Songket Dunia (`songketdunia.my`, Jost), Dentelle (`dentellebridal.com`, Figtree/Nunito). **The one that uses a serif is `thegownatelier.com` — Cormorant Garamond — and it is Malaysia's exclusive Vera Wang Bride stockist.** The serif arrives with the Western label. Consequence: the proposed register is **"a publication of record, warmly printed"**, with the serif confined to the masthead and the article `<h1>` and the rest of the page set in a neutral sans. **This also resolves decision 111's recorded contradiction honestly rather than by fiat: the MARK carries the whitespace, the PAGE carries the density.** A masthead is supposed to be mostly air; a record underneath it is supposed to be dense. Two jobs at two scales, so both halves of the CEO's overruled objection can be true at once.

126. **⚠ THE LOCKED PALETTE IS INDEPENDENTLY CONFIRMED BY THE MARKET — the strongest single piece of evidence in DES-01.** `songketdunia.my`, a brand whose entire product IS songket, grounds its site on **`#f4eee1` (31 uses) with `#302205` ink (22 uses)** and **uses no motif as ornament anywhere on the page** (fetched 28 Ogos 2026). Our locked palette is `#EDEAE1` parchment with `#16130F` ink. Same decision, reached separately, by the brand with the most licence to do otherwise. The decision log previously recorded the palette as a *choice*; it is now a *confirmed* one, and rule 3 of the creative-director persona ("no ornament as identity") has market evidence behind it rather than only taste.

127. **WEBFONT BUDGET CUT FROM FOUR FACES TO ONE — DES-04's own F3 falsifier is promoted from fallback to primary specification.** The register drafted in `tokens.css` on `feat/des-05-design-system-reference` names Fraunces, Archivo, IBM Plex Mono and Bodoni Moda; DES-04 priced that at **120–200 KB against a site shipping ZERO webfont bytes on purpose**, for an audience its own comments describe as low-end Android on slow data. **Decided: one display serif, one weight, subsetted, on the masthead and `<h1>` only (~20–30 KB). Body keeps the system stack. The monospace is dropped entirely** — the rate tables need `font-variant-numeric: tabular-nums`, which system stacks already provide. Reasoning: **the body's problem is not its typeface.** Measured on the live article 28 Ogos — body is **14px at line-height 1.5** in a container up to **1,248px** wide with **no measure applied**. Fixing that to 17px/1.65 at a 68ch cap costs zero bytes and recovers most of the readability; a body webfont spends the whole budget on the element readers are least able to name. **Two people reached this answer from different directions — the DSE wrote it as a falsifier, the CD arrived at it from the market — which is the signal to promote it.**

128. **⚠ THREE LIVE DEFECTS FOUND ON THE HIGHEST-IMPRESSION ARTICLE, and one of them tells us how the current system thinks.** Measured from the delivered HTML and shipped CSS of `/artikel/hantaran-mas-kahwin/mas-kahwin-ikut-negeri`, 28 Ogos 2026. (a) **Two `<h1>` elements with identical text** — `text-[1.75rem]` and `text-[2.5rem]`, one per breakpoint, both in the DOM. **The current system solves responsive type by duplicating the DOM node**, so any scale DES-03 specifies must be FLUID or this returns. (b) **The heading outline is `h1 → h1 → h3 ×13`** — the page's only `<h2>` is the related-articles module. (c) **`.hk-measure{max-width:40rem}` is defined in the shipped stylesheet and used ZERO times**; the body is `<div class="inspire-prose max-w-none">`. Someone specified the measure and nobody applied it. All three are live findings for DES-09 today, not redesign risks for later. **Separately: `.hk-display` already sets `letter-spacing:-.022em`, `text-wrap:balance` and `line-height:1.08`, and resolves to Georgia. The site already has the editorial typography and is missing only the face** — which makes this redesign narrower than the brief assumed.

129. **⚠ TWO THINGS NEED THE OWNER AND ONE PIECE OF EVIDENCE IS MISSING, all stated rather than assumed.** (a) **Dark mode is a live collision.** `globals.css` records a deliberate decision of **2026-07-14**: the dark palette is maintained and correct but there is **no ThemeProvider and no class toggle on `<html>`**, so dark is not user-reachable — while DES-03 and DES-05 both require light AND dark. Reverse the decision or amend the two DoDs; DES-01 did not choose. (b) ~~**`partyslate.com` COULD NOT BE FETCHED**~~ — **CLOSED the same day.** A scripted fetch got 202-with-empty-body then 403, so the team lead measured it in Chrome at 1512×771 instead. See decision 130; the screenshot is committed at `docs/work-done/aug-28-2026-session-01/aug-28-2026-des-01-EVIDENCE/partyslate-homepage-2026-08-28.jpg`. `aliabastamam.com` and `mimpikita.com.my` were also captured in a browser, and **the two captures are NOT of equal value — the rationale says so at the point each is used.** Mimpikita is **partial** (a sign-up modal covers the centre, nothing below the fold loaded); its wordmark reads as a **bold uppercase geometric sans over a terracotta/camel/sand hero**, read BY EYE from the image rather than from computed styles, and it is treated as corroboration that no conclusion rests on. **Alia Bastamam did not render its content at all and NO claim about that brand is made anywhere** — it appears only as a house Grazia MY profiles, with Grazia cited. (c) **The DES-02 brief in `sprint-03.json` still carried the RETIRED "Human photography only" wording** — superseded 26 Ogos 2026 — and has been corrected in this change. **The general rule from that: when a directive is superseded, grep for its old wording and fix every copy in the same change.** A correction that lives in one file and not the others is not a correction.

130. **⚠ THE OWNER'S TWO TOUCHSTONES DISAGREE ON THE FACE AND AGREE ON THE DISCIPLINE — which sharpens decision 125 rather than contradicting it.** `partyslate.com` measured in Chrome at 1512×771 on 28 Ogos 2026, computed styles read off the delivered page after a scripted fetch was refused (202-with-empty-body, then 403). **Every structural heading is Gotham A at weight 500 — h1 and h2 at 24px and 16px, h3 at 16px — and there is NO structural serif on the page.** Gotham A loads at **three weights only (300/400/500), no bold anywhere**. Georgia appears on 89 elements, **11 of them italic**, as the ACCENT voice — the small italic "find" over the hero and the gold italic photo credit — never as display. Six text colours, five backgrounds, **exactly one accent: gold `#B69853` on 8 elements**. Against Carats & Cake's `ivyora-display` serif at ONE weight and two hex colours. **Two premium references named by the same owner, opposite answers on the face, identical answers on restraint: one dominant family, weights in single figures, colours in single figures, one accent, photography carrying the feeling.** Consequence: **the transferable thing is the restraint, not the typeface.** This kills the lazy form of decision 125 ("premium means serif") from inside the owner's own reference set while leaving 125's actual claim — which was about the MALAYSIAN market, where the serif travels with the Western label — untouched, since PartySlate is American and does not test it. It also **strengthens decision 127's four-faces-to-one cut**: neither reference buys a large type library, and restraint costs nothing. **Guard against a false parallel:** PartySlate ships two `<h1>`s, but they are two DIFFERENT section headings; ours are IDENTICAL text duplicated per breakpoint. Decision 128(a) stands unchanged. Same for the two nav rows — theirs are hierarchical, ours carry two different taxonomies per decision 76.

131. **⚠ PROCESS FINDING, AND IT IS ABOUT ME: "recorded honestly as missing" is not the same as closed, and on a gate item the standard is closed.** DES-01 was reported done with `partyslate.com` flagged as unfetched. The flag was accurate and the item passed anyway. When the gap was actually filled it **contradicted the face half of the headline finding** — had it landed after DES-02, it would have arrived as a challenge to three drawn comps instead of a correction to one paragraph. **The rule: an unfetched source that could DISAGREE with you is an open risk, not a footnote. Transparency about a hole does not fill it.** Recorded because the failure shape is attractive — declaring a gap feels like rigour and reads like rigour, and it still shipped an argument that had only met its corroborating evidence.

132. **⚠ CORRECTION TO DECISION 76 — THE SITE HAS SEARCH. IT ANSWERS 29% OF REAL DEMAND, AND "no search anywhere" SENT DES-06 AT THE WRONG PROBLEM.** Decision 76 recorded "no search anywhere", and `sprint-03.json` carries it into DES-06's title and `why`. Measured on 28 Ogos against the deployed bundle: a `InspireArticleSearch` combobox exists on `/artikel`, backed by `GET /api/v1/search`, reached from a header magnifier on every page via `/artikel#cari`. **The fault is not absence, it is the matching rule** — it requires the query to appear as a **contiguous substring** of a title or excerpt. **All 248 GSC queries from 31 Jul–27 Aug were put through the site's own API: 209 return ZERO (84.3% of queries, 70.9% of impressions).** `mas kahwin terengganu` — 35 impressions, 3 clicks, position 6.1, **27% of every click the site earned in those 28 days** — returns zero, because its article is titled "Mas kahwin Kelantan dan Terengganu 2026" and the phrase does not appear in that order. `terengganu` alone returns it. Simulated three-stage token matching lifts answered demand **29.1% → 76.7%**; the residual 23.3% is content that does not exist (`pusat komuniti setiawangsa`, 185 impressions). **Two further defects, both measured:** a failed request and a genuine zero-result render the same string, so a dropped connection tells a reader the publication has nothing on their topic; and the focus ring composites to `#b7b6b4`, **1.96:1** against `--background`, under the 3:1 WCAG 2.2 floor. Reproducible: `python docs/design/des-06-evidence/reproduce.py`. **The general finding, which is bigger than search:** the company had never measured a shipped feature against its own GSC demand, and the audit that found this took under an hour. *"There is no X"* and *"X is there and does not work"* need different items, and only one of them was in the sprint.

133. **THE CATALOGUE'S FAILURE IS BROWSE, NOT ONLY SEARCH — and one live empty state is FALSE.** Counted from `sitemap.xml` and the fifteen rendered category pages, 28 Ogos, both agreeing exactly: **86 articles, 15 category pages, of which only 9 are in the navigation**. `/artikel` links to **12 of the 86**; the other 74 are reachable only through category pages, six of which are unlinked from the nav. Hantaran & Mas Kahwin holds **38 (44% of the corpus)**; eleven categories hold four or fewer and four hold exactly one. **Six sections across five categories still show "akan datang tidak lama lagi", standing since Sprint 02 — and the one on `venue-perancangan` headed *Dewan & venue majlis* is untrue**: six dewan articles are live, two of them further up that same page. A reader who searches "dewan" is served; a reader who browses to the section named for it is told to come back later. DES-06 specifies the replacement; whether an empty section should render at all is **managing-editor's** call and is flagged, not decided.

134. **PAGINATION OVER INFINITE SCROLL, threshold 60 items — argued on crawl, back button and return-to-position, and NOT YET SIGNED BY head-of-seo-content.** DES-06 co-owns this with that seat and DES-09 has not been written, so the reasoning is recorded as a proposal it can overrule. Infinite scroll leaves items past the first page with no crawlable link, and making it crawlable means building paginated URLs underneath it anyway — a pattern whose default failure mode is hiding links from crawlers, directly against SEO-02's removal of 79 `rel=nofollow` and addition of 68 real links. Back and return-to-position are free with `?page=n` and are restoration code that silently breaks with infinite scroll. **Below 60 items neither mechanism ships**: the largest set on the site is 38 and already renders in full. Two further items need the same signature: `/cari` as `noindex,follow` and robots-disallowed, and the `/artikel` index change, which would add one inbound internal link to all 86 articles.


135. **⚠ THE REGISTER'S SPINE MOVES FROM THE TYPEFACE TO COLOUR COUNT — because that is the ONLY thing every reference agrees on, and it survives the touchstone disagreement.** `aliabastamam.com` and `mimpikita.com.my` were measured in Chrome on 28 Ogos after both refused a scripted fetch (403), computed styles walked across every text-bearing element. **Alia Bastamam: Montserrat on 265 elements, FIVE text colours, FOUR backgrounds, body `#262626` on `#F9FAF8`. Mimpikita: Inter on 782 elements, FIVE text colours, FIVE backgrounds, body `#3D2F16` on `#FFFFFF`, h1 Inter 36.94px w400.** The `Times` counts in both (40 and 55) are an **unstyled browser fallback, not a chosen face** — recorded because reading them as a second typeface would invent a serif nobody selected. **(a) Decision 125's count strengthens: SIX OF SEVEN** measured Malaysian premium bridal brands on geometric or humanist sans, the single serif still attached to the Vera Wang stockist. **(b) The new spine:** Carats + Cake 2 colours, Alia Bastamam 5, Mimpikita 5, Dentelle 5, PartySlate 6 — against nikahsatu 50 and mingguanwanita 66. The references agree on nothing about face, weight or temperature and agree completely that a page uses very few colours. **The serif/sans finding says what not to copy; the count finding says what to do, and it is free.** A register built on count discipline survives the owner changing their mind about the typeface, which decision 125 alone does not. *(The two counts are different measurements — computed text colours versus hex literals in delivered markup — and the rationale keeps them in separate columns rather than pooling them.)*

136. **⚠ A THREE-BRAND "WARM INK" CONVERGENCE WAS PROPOSED AND THE ARITHMETIC KILLED A THIRD OF IT — recorded because the failure mode is a colour claim stated as an adjective.** Red-minus-blue computed on the 0–255 channels: Songket Dunia `#302205` **+43**, Mimpikita `#3D2F16` **+39**, HelloKahwin locked `#16130F` **+7** — but **Alia Bastamam `#262626` is R=G=B, DEAD NEUTRAL, not warm black**, and its secondary ground `#F4F7FB` is faintly *cool*. Reporting it as a third warm-ink brand would have manufactured a pattern from two real points and one pointing the other way. **The real finding is better: warmth tracks CULTURAL PROXIMITY the same way the serif tracks Western proximity.** The two brands rooted in traditional craft run warm ink; the Western touchstones and the most Western-facing Malaysian house all run neutral ink at exactly 0 (PartySlate and Carats + Cake both use pure `#000000`). Our locked ink at +7 sits deliberately between them — the temperature of paper, not of a product shot. **One claim does hold across all six: NO measured brand pairs pure black with pure white.** Both touchstones use true black on an off-white ground; Mimpikita uses true white with warm brown-black ink; every one softens at least one side, and ours softens both. That is the evidence behind the "no pure black, no pure white" entry in the NOT-do list. **Standing rule: when a colour claim arrives as an adjective, compute the channels before repeating it.** "Warm" survives review by not being checkable.

137. **⚠ A CENSUS CAN BE SOUND IN ONE DIMENSION AND JUNK IN ANOTHER — grade it per measurement, not as a source.** The `aliabastamam.com` and `mimpikita.com.my` computed-style censuses both returned **impossible body sizes — 9.75px and 9px.** No production site sets a 9px body, so a non-default root size or an ancestor transform is scaling the cascade. **All type SIZES from those two pages are withheld from the DES-01 rationale**, including an h1 figure that had already been quoted in two places and has been removed. **What survives untouched is the family census and the colour counts** — a `font-family` name and a `color` value do not change under a scale transform, and 265 and 782 styled text elements were present to count either way. The same page also failed to load its lazy images, which is a third, separate limitation affecting only composition claims, of which none are made. **Three limitations, three different scopes, one source.** The first instinct — call the whole source weak and rest nothing on it — would have discarded the sixth and seventh brands in decision 135's count and the two 5-colour data points in its spine. **Grading a source as a whole is faster and loses real evidence.** PartySlate is not affected: its sizes are internally consistent (24px and 16px headings against a 16px body at 24px leading) and it rendered fully.

138. **THE DISPLAY FACE IS DECIDED AND FINAL: Bodoni Moda 2.005, `wght 400`. The wordmark stops being provisional, and DES-11 and DES-12 are unblocked.** Licence **SIL Open Font License 1.1**, read from `google/fonts@main`, `ofl/bodonimoda/OFL.txt`, 28 Ogos 2026. Cost **RM0**, perpetual, no pageview tier, no account, no renewal. The two clauses that decide it: *"Permission is hereby granted, free of charge … to use, study, copy, merge, embed, modify, redistribute"*, and *"The requirement for fonts to remain under this license does not apply to any document created using the fonts or their derivatives"* — the second is what lets an outlined SVG lockup ship with no licence travelling in it. Coverage is complete and, per the DoD's own warning, uninteresting: **all 69 codepoints the site's display type sets are present, and so is all 128 of Latin Extended-A.**

139. **⚠ `ivyora-display` IS REJECTED ON THE LICENCE, NOT THE PRICE — and Adobe's FAQ and Adobe's contract do not say the same thing.** From the **Adobe Fonts Service Product Specific Terms, effective 7 Oct 2024** (fetched from Adobe 28 Ogos, copy in evidence): §3.1(B) *"You may reference or encode a link to the Web Project within your Website's design.* **No other web usage is permitted.**"; §3.4(E)(1) prohibits *"hosting the Licensed Content on your own server or other self-hosting option or service"*; the grant in §3.1 runs *"only for as long as you maintain an uninterrupted Adobe Subscription Plan"*. The webfont FAQ (Internet Archive, 10 Apr 2026) is blunter: *"Any other method of displaying the font on your website isn't allowed"* and *"If you cancel your Creative Cloud subscription, the web fonts will no longer be available to your websites."* **Two concrete costs, not abstractions.** (a) DES-01's specification — one display serif, subsetted, self-hosted, preloaded — becomes illegal; the only permitted delivery is a third-party request to `use.typekit.net` at `font-display:auto`, in front of the LCP element, on an audience the code's own comments call low-end Android on slow data, and DES-09 holds a veto over exactly that. (b) **The FAQ (Internet Archive, 25 Aug 2026) expressly permits the outcome** — *"you can modify or decorate type that has been rasterized or converted to outlines … may be copyrighted, registered as a trademark"* and outlined text *"may be reproduced and distributed independent of your subscription status"* — **but §3.4(E)(4) and (E)(7) fence off our road to it**: decision 114's marks were made by reading a font binary with fontTools, and converting the Licensed Fonts and copying them out of Adobe's install folder are both named prohibitions. The permitted route is setting type in an Adobe app and outlining it there, by hand, every time the tracking is re-solved. **The objection is not cost. It is that Adobe Fonts makes the brand's most-repeated maintenance operation manual and rents the `<h1>` by the month.** Costs recorded for completeness and both are dated rather than live, because `adobe.com` refused every request path tried today: **RM166.69/mo All Apps incl. SST** (adobe.com/my_en, Internet Archive 23 Jun 2025) and **RM220.32/mo standard** (Lowyat.NET, Apr 2026) — RM2,000–2,644 a year against zero. **The escalation route, if the owner still wants the Carats & Cake face, is Ivy Foundry DIRECT, not Adobe:** IvyOra Display one style desktop **USD 29.99 ≈ RM121**, ten-style family USD 197.99 ≈ RM798, 22-style family USD 317.99 ≈ RM1,282 (schema.org product data in the page delivered 28 Ogos; USD→MYR 4.033, ECB reference 27 Aug). Their terms permit self-hosting from WOFF2 and permit logos outlined before export — the things Adobe's forbid.

140. **⚠ THE MALAY DIACRITIC GATE IS NOT MERELY WEAK, IT IS EMPTY: the site's display type contains ZERO diacritics.** Every `<h1>`, `<h2>`, `<h3>` and `<title>` across seven production pages fetched 28 Ogos, plus all 86 article titles and 15 category names from DES-06's corpus, plus the wordmark string: **69 distinct codepoints, of which exactly two are non-ASCII — en dash `U+2013` and em dash `U+2014`.** Not one accented letter anywhere. Decision 117 demoted the gate from three personas; the measurement says it cannot discriminate between any two shipping Latin typefaces at all. **Correction to decision 117 in passing: it says the gate was written into THREE personas. A machine-wide grep returns TWO** (`creative-director.md`, `design-systems-engineer.md`); the only third document containing "diacritic" is the graphic-template-kit spec of 24 Aug, which says *"diacritic-free"* and already agrees. **The correction to an overstatement was itself overstated by 50%, inside the entry that named overstatement as the failure shape.** Both personas corrected in this change.

141. **⚠ THE FACE NAME WAS TRUE AND THE SPECIFICATION WAS STILL MISSING — the five shipped marks were cut at an optical size nobody chose, and it puts the primary lockup under the WCAG non-text floor.** The `H` in `hellokahwin-horizontal.svg` has x coordinates `62, 242, 433, 632, 892, 1092, 1282, 1462`. Bodoni Moda's own `H`, normalised to a 1500-unit cap height, matches **exactly at `opsz 11, wght 400`** and at no other instance tested (`opsz 6` gives 86, 267, 457…; `opsz 96` gives 37, 216, 407…). So the marks genuinely are Bodoni Moda — **and `opsz 11` is the font's own `registry_default_overrides` value in Google Fonts' `METADATA.pb`, meaning the optical size was never selected.** That is not cosmetic on this face: across `opsz` 6→96 at `wght 400` the **thick stroke moves 201→210 units (4%) while the hairline moves 72→4 (a 94% collapse)** and stroke contrast runs 2.79:1 to 52.50:1. **Consequence, computed:** a vector stroke under one device pixel antialiases to ink composited at alpha = its width, so at the brand page's own stated 18px minimum on a **DPR 1** display the shipped hairline is 0.46px and composites `#16130F` to `#8B8881` — **2.94:1 against parchment, under the 3:1 floor of WCAG 2.2 §1.4.11.** Re-cut at `opsz 6` the same mark composites to `#33302C`, **10.91:1**, for zero bytes and zero ringgit. Scope stated rather than dramatised: this is a **desktop** failure; at DPR 2 and 3, where 64% of impressions arrive, it clears. **The general rule, and it is the transferable part: naming a variable font does not name a typeface. Identity is `family + version + axis coordinates`, and anything less is a label.** **The re-cut is reopened as DES-10 — which is the creative-director's own item and owns the five lockups — with a per-lockup instance table, rather than done inside this one.** DES-11 ships those files on the brand page; it does not cut them, and routing a creative-director job to a direct report inside the permanent record would be wrong. The face is unchanged, so the DoD's regeneration trigger does not fire; the branch also sits in front of the owner awaiting merge and was deliberately not rewritten underneath them. DES-11 and DES-12 are re-pointed at `blocked-by-DES-10`. Set width moves +2.26% at `opsz 6`, which the solved tracking absorbs, so 10.0:1 holds.

142. **THE WEBFONT IS PRICED IN BYTES AND FITS: 21,388 bytes.** Decision 127 set the budget at one display serif, one weight, subsetted, ~20–30 KB, against a site shipping zero webfont bytes on purpose — re-confirmed 28 Ogos from the delivered artefact rather than the source: **0 `woff`/`woff2` references and 0 `@font-face` rules across seven production pages, and 0 `@font-face` in the 141,795-byte stylesheet the browser actually downloads**, where `--font-cormorant` resolves to `Georgia,"Times New Roman",Times,serif`. Measured subsets of Bodoni Moda in woff2: site set only with axes kept 26,152; site set with `opsz` pinned 14,216; **site set + Latin-1 + register punctuation with `opsz` pinned 11 — 219 glyphs, 21,388 bytes**, which is what ships; the same with axes kept, 39,028, which does not. Google's own `latin` subset for this family is 53,756 bytes. **Pinning the optical size is what brings the file inside the budget at all — it saves 17,640 bytes for a range the `<h1>` never uses.** Self-hosted, preloaded, `font-display: swap`, on the masthead and `<h1>` only. Full argument, five falsifiers and the per-lockup instance table: `docs/work-done/aug-28-2026-session-01/aug-28-2026-done-des-13-display-typeface.md`.

## 2026-08-27 — Owner directive: the premium redesign, and a Design department

100. **⚠ OWNER DIRECTIVE — a premium redesign of the homepage, catalogue and article pages, plus a real design system.** References named by the owner: caratsandcake.com and partyslate.com. **The site today runs Tailwind + shadcn defaults on Geist** (`bg-background`/`text-foreground`, lucide icons) — verified against live HTML on 27 Aug — which is the generic AI-startup look and the thing this work exists to replace.

101. **POSITIONED AS EDITORIAL PREMIUM, NOT A VENDOR MARKETPLACE — and the owner chose this against the grain of their own references.** Both named sites are vendor MARKETPLACES: PartySlate and Carats & Cake monetise venue and vendor listings, not content. Presented with that, the owner chose to keep the content model and treat monetisation as a separate later decision. **Recorded prominently because the references will otherwise pull the design toward a business model we have not chosen** — a catalogue page designed as a vendor directory is a different product.

102. **⚠ THE TENSION IS RECORDED RATHER THAN RESOLVED: this work is NOT justified on SEO grounds and must not be scored on them.** Decision 86 measured every category URL at ~16 impressions and ZERO clicks across 30 days — 0.6% of site impressions — and Sprint 03 originally placed a UI overhaul OUT of scope on exactly that basis. The owner has overridden that, correctly on its own terms: **a premium redesign is a brand and positioning bet, and decision 86 measures clicks.** The objection does not apply to the objective. It is written down so that a later meeting does not score this against traffic and call it a failure.

103. **A DESIGN DEPARTMENT WAS CREATED — the org chart had NO design function at all.** Three seats hired 27 Aug into `agents/projects/hellokahwin/Design/`: **`creative-director`** (taste, art direction, page composition; writes no code), **`design-systems-engineer`** (tokens, components, the reference page; writes production code), **`product-designer`** (flows, search, browse, and the states nobody designs). BMAD's generic UX agent was not sufficient — it carries no brand or art-direction mandate and no Malay-audience context.

104. **Authority split, decided with the owner: the OWNER picks the art direction, ONE gate, early.** Everything downstream of that choice — type scale, colour ramps, spacing rhythm, photographic selects, component styling, page composition — is the Creative Director's without further approval. Rejected: full standing autonomy over taste (too high variance on the thing the owner cares most about) and per-page sign-off (three approval gates inside one sprint, and three stalls whenever the owner is away).

105. **The CD must RESEARCH and DEFEND a Malay-premium register before drawing anything (DES-01 is a gate).** The brief could say what premium must NOT be — Western bridal-magazine pastiche wearing Malay words, and motif-as-ornament — but had no positive definition, and the owner declined to supply one. So deliverable one is a rationale with **real evidence from named Malaysian brands and actual SERP competitors**, and the owner approves the REASONING before a comp exists. Taste arguments are unwinnable in the abstract and cheap to settle against evidence.

106. **THE STACK DECISION WAS DELIBERATELY LEFT OPEN TO THE ENGINEER (DES-04 is a gate).** The owner declined to pre-decide whether Tailwind stays. It is argued against the art direction once that exists, not against a hypothetical one. **The brief requires the two questions to be answered SEPARATELY** — does Tailwind stay, and do shadcn/Radix primitives stay — because conflating "shadcn looks generic" with "Radix accessibility must go" is how teams throw away accessibility to fix a look.

107. **The design system lives INSIDE the hellokahwin site repo** (`src/design-system/` with a private `/design-system` reference page), not as a versioned package. Basis: there is exactly one consumer today, and buddy already does precisely this. **The reference page is specified as the regression test for taste, not documentation** — it reads tokens from the system itself and updates in the SAME change as any token or component.

108. **SEARCH ENTERS SCOPE AS A DESIGN ITEM, and its BUILD is deliberately excluded.** Decision 76 found no search anywhere on a site whose demand is overwhelmingly named-entity lookups. DES-06 specifies it; the implementation is a separate item to be sized once the design exists. The owner chose this over both "design and build it now" (grows the sprint further) and "leave it out" (which would make the catalogue redesign close to meaningless).

109. **Sprint 03 grows 52 → 97 points, 13 → 21 items, and is RENAMED** *"Convert the impressions we now have — and stop looking generic"*. Design is now the largest track at 45 points. **This is the second consecutive sprint to grow after approval, and as in Sprint 02 the growth came from evidence and directives produced AFTER planning rather than from scope creep inside it** — that is the pattern the Sprint 02 retro said to keep. A sprint is a body of work with no clock, so the number is a measurement, not a budget.

110. **Sprint 03 has still not started. Nothing is dispatched, zero of 21 items are done.** Recorded because the owner asked whether the sprint was complete: **Sprint 02 completed; Sprint 03 has never begun.** The redesign is therefore being added to an unstarted sprint, which is the good case — no rework, and the design track can be sequenced against SEO-07 deliberately rather than retrofitted.

## 2026-08-26 — Sprint 02 planning: the publishing hole, and the click we do not earn

> **Gap noted and closed here:** Sprint 01 (25–26 Aug) produced no entries in this
> log and no meeting minutes. Its decisions are not lost — they live in
> `docs/sprints/sprint-01.json`, `sprint-01-retro.json` and
> `reports/sprint-01.html`, each with evidence. But a sprint is where most
> decisions now get taken, and this log stopped at 24 Aug while the company kept
> deciding. Recorded rather than back-filled, because inventing numbered entries
> after the fact would put reconstruction into a log meant to hold
> contemporaneous record.

62. **Sprint 02 opened at 35 points, 10 items** — *Close the publishing hole, then earn a click*. Owner set scope at the full proposal; the CEO's own cut list (PLAT-06 and SEO-05) was declined. Velocity is computed from item data, not typed.
63. **⚠ RISK-04 IS THE GATE: publishing does not tell Google.** Sprint 01 published five articles Google has never heard of — `sirih-junjung`, `dulang-hantaran`, `gubahan-hantaran`, `walimatul-urus` all return coverage_state "URL is unknown to Google", last crawled **Never**. All four are in the live sitemap (78 URLs). **Google last fetched that sitemap at 73 URLs on 25 Aug 15:58 — before they existed.** The ingest CLI purges our own caches and stops there. Every content point in this sprint is worthless until this closes, which is why it runs first.
64. **The Indexing API is NOT used, and the CEO withdrew it mid-meeting.** It was in the RISK-04 proposal. Google restricts it to `JobPosting` and `BroadcastEvent`; using it for articles is a policy violation, not a clever shortcut. The sanctioned mechanism is sitemap resubmission with accurate `lastmod` — exactly what we fail to do. Recorded because the error was attractive and would have shipped.
65. **RISK-05 — an indexing monitor, not another baseline.** Murat: the CEO found five dark articles by hand, today, because it happened to run a URL inspection. SEO-01 was a one-off. **A baseline you take once is a photograph.** The alarm fires when any sitemap URL is unknown or uncrawled 72h after appearing, and it runs from the DEFAULT branch — scheduled workflows do not run from feature branches, which cost a reopen in Sprint 01.
66. **Sprint 01's crawl work is CONFIRMED LANDED.** Six of six formerly-uncrawled articles now report "Submitted and indexed", crawled 23–26 Aug, against an SEO-01 baseline of 8 of 28 indexed with 19 discovered-never-crawled. Removing 79 `rel=nofollow` internal links and adding 68 real ones did what SEO-02 predicted. **Stated as a sample of six, not a census** — reporting it as "all 28 now indexed" is exactly the error Sprint 01's retro was written about.
67. **⚠ INDEXING WAS NEVER THE BUSINESS PROBLEM — it was the prerequisite.** Head of SEO, pressing the point the CEO would have underweighted: `dewan komuniti setiawangsa` is indexed, crawled, ranks **8.8**, carries **34 impressions**, earns **zero clicks**. `dewan keramat` ranks **4.5** and earns zero. `hantaran-tunang`: 44 impressions at 9.8, zero. The new `/artikel/idea-dan-nasihat/dewan-kahwin` sits at **7.6 with zero clicks** while the **legacy** `/dewan-kahwin/` at a *worse* 9.9 takes **11 of the site's 15 weekly clicks**. We have proven we can get indexed and have not proven we can earn a click.
68. **The venue page type (SEO-04) enters the plan, modifying the 23 Aug cluster plan.** The query list is dominated by NAMED HALLS — setiawangsa, keramat, mpaj tasik tambahan, perdana keramat mall, shah alam. People search for a specific hall; we answer with a generic listicle. Head of SEO: that is a page-type problem, not a ranking problem.
69. **Owner decision: content runs BOTH lines** — venue pages *and* the cluster queue, not one or the other. The CEO proposed pausing clusters; the owner declined. Safe because RISK-04 closes the publishing hole in the same sprint.
70. **⚠ CEO ERROR: decision 50 was assigned on 24 Aug and never followed up.** `dewan komuniti setiawangsa` was 25 impressions at position 8.9 then, flagged as a live already-ranking opportunity and handed to `head-of-seo-content`. Two days later: 34 impressions at 8.8, still zero clicks. **The assignment produced nothing and the CEO never checked.** An assignment nobody audits is a note, not a decision.
71. **⚠ CEO ERROR: the Sprint 01 close reported "both repos clean" having checked two of three trees.** The site worktree — the one holding the site code — carried **93 uncommitted files**. Among them: the **UNDO scripts for every production database write Sprint 01 made** (P1/P6, P3/P4/P7, P5, the C2.4 cover swap, the text-card purge, CONT-02's 69-image enrichment), `src/lib/inspire/text-card.ts` and its tests (live code, imported by the tool enforcing the no-text-cards rule), and three scripts the already-committed `package.json` references — so a fresh clone would fail on all three. RISK-01 gives a database restore; those UNDO files are what let us reverse *one* write without restoring everything, and they existed only on one laptop. Committed at `7597ea9`. **Found while closing Orca terminals, not by any check** — the same class of failure as the three unshipped items, committed by the CEO while writing the retrospective about it.
72. **PLAT-05 widened from sprint documents to all 422, and split in two.** Owner direction: an Atlassian-like place for all our documents, and make sure retros are stored. The old spec covered ~20 sprint documents and **did not name retros at all**, so they would have been missed a second time. PLAT-05 is the engine (sync, store, search, render); PLAT-06 is the Confluence-like navigation (spaces, path-derived tree, breadcrumbs, backlinks). Each independently shippable.
73. **The secret-shape guard SKIPS and continues; it does not refuse and abort.** The prior round table found it would reject the `/tokens` registry — the document most worth finding. At 20 documents a hard refusal is an inconvenience; **at 422 it stores nothing and shows the operator a stack trace.** It now names the file with pattern and line number and finishes the run.
74. **The document page tree is derived from file paths, never hand-arranged.** Confluence's hand-arranged trees are stale in every long-lived instance and nobody notices. Ours re-derives on every sync so it cannot drift; the cost is that we cannot organise better than the directories are organised. Accepted — a wrong tree is worse than a plain one.
75. **PLAT-09 — the buddy sidebar carries the latest additions.** Owner request. `/sprints` shipped in Sprint 01 and reaching it means typing the URL; `/docs` arrives with PLAT-05 and would inherit the problem. **A surface nobody can navigate to is close to a surface that does not exist** — two of Sprint 01's three unshipped items hid behind exactly that.
76. **UI/UX review commissioned on the homepage, category and article pages**, to a UX seat and the Head of SEO in parallel, reporting after planning so any work is added deliberately rather than absorbed. The CEO's own visual pass already found: a **grey placeholder card** on the homepage for `dulang-hantaran` while the article's own page renders its cover fine (a card-variant failure, not a missing image); **two navigation bars carrying different category taxonomies**; nav text truncated to "BUSANA & PENAMPILAN PENGAN"; a category page with **no images at all** that shows **two "coming soon" empty states above its real content**; covers that are human photographs per the owner's rule but **do not depict the article's subject**; and **no search anywhere**, on a site whose demand is named-entity lookups.
77. **Orca terminals 26 → 1.** All 26 idle, nothing WORKING or DELEGATING, idle 13–18 hours, all Sprint 01 work. 24 closed; two refusals were already gone with their tab. The one remaining is the live session. **Verifying nothing uncommitted was at risk before closing is how decision 71 was found** — the cleanup paid for itself.
78. **Process defect found: `status-board.py` crashes on Windows.** `UnicodeEncodeError: charmap codec cannot encode character U+2733` — the Orca title glyph against cp1252. It runs only with `PYTHONIOENCODING=utf-8` set. **The dispatch watcher depends on this script**, so on a default Windows shell the watcher is broken. Not yet fixed; recorded so it is not rediscovered.

79. **⚠ THE HEADLINE FINDING OF THIS MEETING WAS A MEASUREMENT ARTIFACT, AND IT WAS THE CEO'S.** The CEO reported that the legacy `/dewan-kahwin/` "out-converts" its replacement from a worse position, and built a brand-and-title theory on it. **They are one page.** URL Inspection: the legacy URL is *Page with redirect* with a Google-selected canonical pointing at `/artikel/idea-dan-nasihat/dewan-kahwin` — the same canonical the new URL reports, same crawl timestamp (2026-08-23 01:09). **GSC attributes an impression to the URL string Google printed in the SERP, not to the canonical.** Added together the real page is 34 clicks / 1,090 impressions / **3.1% CTR at position 9.3 — above the curve.** Our best page is working. Consequence to hold: **89% of the site's search presence (2,318 of 2,605 impressions) is attributed to three legacy URL strings.** Every dashboard is measuring pre-migration strings. It resolves itself — but never read two rows as two pages.
80. **⚠ "WE RANK AND DO NOT CONVERT" IS RETRACTED.** Sitewide CTR is **1.65% at average position 20** — at or slightly above the published curve. The best page runs 3.1% at 9.3, above curve. **There is no conversion problem. There is a POSITION and VOLUME problem** — we are a small site sitting on page two, and the fix is more pages and better rankings. Titles are worth fixing because they are cheap, not because they are the bottleneck. The CEO's framing would have pushed the team to optimise something already trending correctly.
81. **⚠ AND THE WEEK THE CEO PRESENTED AS A PROBLEM WAS A GOOD WEEK.** 15 clicks on 765 impressions = **1.96% CTR, better than the 30-day 1.65%.** Impressions rose **88/day → 165/day**; average position improved **21.4 → 14.7** across the month. **Sprint 01 is working.** A 7-day, 15-click window on a ~40-clicks-a-month site is noise, and the CEO read a conversion failure into it.
82. **SEO-05 was re-pointed TWICE in one meeting, both times before work started.** v1 ("audit every page at position <10 with zero clicks") was noise — at 25–50 impressions expected clicks are 0.3–1.5. v2 ("dewan komuniti setiawangsa entity-match") was **also wrong, and was killed by the item's own owner**: 104 impressions at position 9.0 predicts ~2.1 clicks, P(zero) ≈ 12%, on a SERP where 1–2 is DBKL's own booking portal and 3–6 are Instagram, Facebook, Waze and TikTok. The owner's line is the one to keep: *the UX review applied a significance test and then exempted the single page it had already built a theory around.* **The real zero is `mas kahwin ikut negeri`** — 388 impressions across three URLs of one redirect family, zero clicks, expected ~5.8, **P(zero) ≈ 0.3%** — invisible until the family is aggregated. v1 also asked for something unbuildable: one 60-character listicle title cannot entity-match seventeen distinct hall names.
83. **⚠ SEO-04 AIMED AT THE WRONG ENTITIES — the CEO's own finding, corrected by competitor data the CEO never pulled.** The four named halls are **council** halls totalling **~30 searches/month** (setiawangsa 10, perdana keramat mall 20, keramat 0, mpaj tasik tambahan 0), with positions 1–2 held by `tempahkl.dbkl.gov.my` at DR 64. **We could have built them perfectly and earned nothing** — structural, not a template problem. Against nikahsatu.com (DR 14): **entity pages ~4,900 traffic vs ~293 for geographic hubs, roughly 17x**, and its winners are commercial venue brands at 500–2,400 volume with no official portal above them. Re-scoped to **8+ entity pages across 2+ multi-location commercial brands**, gated on a one-hour sourcing check that STOPS the item if real prices and capacities cannot be obtained, with **Setiawangsa retained as a deliberate control** so the council-hall category is proved dead rather than quietly dropped. Expected outcome stated as **position 3–5, not 1**. New standing rule in `head-of-seo-content`: **pick targets on search volume AND who owns position 1 — never on our own GSC impressions**, which show where we already appear rather than where demand is.
84. **RISK-06 — `stale-while-revalidate` is set to 365 days, and it outranks every title we ship this sprint.** Found by the SEO review, **confirmed by the CEO against production**: every article page ships `s-maxage=600, stale-while-revalidate=31535400`, with `mas-kahwin-ikut-negeri` at `X-Vercel-Cache: STALE` Age 787 and `dewan-kahwin` STALE at Age 575. The reviewer caught six pages served from that window carrying the **site-default homepage title**, no canonical, no og tags, while their H1 and JSON-LD were correct. **The CEO could not reproduce it** — six consecutive fetches all HIT — **because its own earlier requests had warmed the cache.** Recorded as **exposure, not confirmed loss**; nobody has proven Google indexed a stale shell. The DoD therefore requires reproducing the symptom deliberately before fixing, forbids reporting it fixed if never reproduced, and ships the header cap on the mechanism alone.
85. **SEO-06 — the Hantaran pillar advertises its own ranking articles as missing.** Three "akan datang tidak lama lagi" empty states, two of which have published, indexed, **ranking** articles filed under the wrong pillar: `hiasan-dekorasi/hantaran-kahwin` at position 6.5 and `hiasan-dekorasi/hantaran-tunang` at 49 impressions position 9.6. The pillar never links down to them. Taxonomy, not design.
86. **Category and pillar pages earn essentially nothing** — ~16 impressions and 0 clicks across every category URL in 30 days, 0.6% of site impressions. They are internal-link plumbing. **Design effort on them returns nothing**; the taxonomy bug behind them (85) is what is worth fixing.
87. **⚠ THE CEO CREATED THE TWO-SOURCES-OF-TRUTH PROBLEM IT HAD JUST SPECCED AGAINST.** SEO-05 was re-pointed in the tracker and not in `docs/sprints/sprint-02.json`, so the item's own owner spent a round arguing against a version already replaced. The file is the frozen scoping snapshot; **the tracker is the state.** Reconciled item-by-item at close — 16 items both sides, no drift — and the file now says so. Also found: `sprint get` **does not display the `why` field at all**, so an edit to it cannot be verified from the CLI. Same write-only class as the retro bug; added to PLAT-07.
88. **Named-hall queries are ~7% of impressions, not the dominant share the CEO claimed.** By ROW COUNT they dominate the query list; by impressions they are ~180–200 of 2,605. English `garden wedding` queries are ~25% — 897 impressions, 4 clicks, position 36.5, on an English-intent page on a Malay-first site. **Counting rows instead of impressions is what made halls look like the story.** The garden-wedding question (keep it, stop measuring it, or drop it) is deferred to Sprint 03 planning with the data attached.

## 2026-08-24 — Routine check-in: standing autonomy granted, and four decisions taken under it

42. **⚠ STANDING AUTONOMY GRANTED BY THE OWNER.** The CEO brought a fully-evidenced production deploy to the board as an approval request. The owner's response: *"What problems do you have making these decisions? I said you have full autonomy over it."* **The CEO decides and executes; the board is reported to, not asked.** Recorded because it corrects a real failure mode — staging a decision as a request when the evidence is already complete is caution theatre, and it makes the CEO the bottleneck of the company. Four things still stop and go to the owner, because only they can supply them: **credentials/access, money, outward-facing commitments in the company's name, and irreversible destruction.** Persona updated so this cannot recur.
43. **Revalidate fix APPROVED AND DEPLOYED to production — CEO decision, no board vote.** Branch `ianng89/pillars-ingest-redirects` @ `105d9de`, 7 commits ahead of `origin/master`, 15 files, +556/−50. Basis: mechanism traced through Next 16.1.6's own source rather than guessed; single-request proof shown as literal output; **45 call sites closed, not the 1 the brief named**; regression guard verified by reintroducing the bug, and hardened after Codex found it could be defeated with `import { revalidateTag as bust }`. Gates: 224 tests, typecheck clean, lint 0 errors, build exit 0. Seven Codex findings — two fixed, five answered with evidence. Condition attached: **repeat the one-request test against production**, because the local proof used Next's filesystem cache handler and Vercel's own handler cannot be exercised without deploying.
44. **The mechanism, recorded because it will bite someone again:** `revalidateTag('articles', 'max')`. The second argument reads like an intensity — *purge as hard as possible*. It is a **cacheLife profile name**, and `max` is a one-year expiry. The tag was marked *stale*, not *expired*, and Next serves a stale entry once before refreshing. **The article's own URL was never the failing surface** — a new slug has no cache entry to go stale. What failed was the pillar's indexability decision: on the first crawl after an ingest the pillar still answered "nothing published beneath me" and served `noindex, follow`. The real defect was an SEO exposure wearing a caching bug's clothes. The ingest CLI meanwhile printed *"Content caches dropped — the article is visible on the site now."* It was not; the route returned 200 while doing nothing, which is why this survived an earlier review.
45. **Edge cache: option (b) — purge the Vercel edge as part of ingest.** `next.config.ts` sets an explicit `Vercel-CDN-Cache-Control` on three routes (`s-maxage=300, swr=600` on pillar and article pages, `s-maxage=3600` on the sitemap), which opts them out of automatic purge-on-revalidate. A second, independent staleness source — not the bug just fixed. Rejected (a) do-nothing-and-time-the-ingest as depending on a human remembering a five-minute stopwatch during a busy week, and (c) dropping the header as reversing a deliberate performance decision on traffic numbers we do not have. **Blocked on one owner-only item: a Vercel API token in the vault.** The engineer escalated rather than deciding unilaterally — correct, and endorsed.
46. **The eight C2.4 articles publish as soon as the production one-request test passes — NOT held for the edge work.** Reasoning: the article's own URL was never the failing surface, the pillar staleness window is five minutes rather than permanent, and seven pillars are sitting on a `noindex` clock that degrades toward `nofollow`. Interim rule with a known expiry: publish, wait five minutes, then invite the crawl — option (a) used as a stopgap, not as the answer.
47. **Post-migration read, now on a second independent sample.** GSC 21–23 Aug vs 15–20 Aug: clicks/day 0.83 → **2.67**, impressions/day 76.3 → **94.3**, CTR 1.09% → **2.83%**, position 18.0 → **15.7**. Every direction right. Three days against six is a small sample and this is directional, not a verdict — but it is the second read pointing the same way and it closes the "did the migration hurt us" question. Sitemap Valid, 39 URLs, 0 errors, downloaded 03:10 today. All seven pillars verified 200 in production, including `nikah-undang-undang`, which was the P1 404 — that fix landed.
48. **⚠ MEASUREMENT RULE: Google is still serving the OLD URLs, and that is where the traffic is.** `/mas-kahwin-ikut-negeri/` 44 impressions at position 9.6 versus 5 on the new `/artikel/...` path; `/dewan-kahwin/` 132 impressions and 5 clicks at 9.4 versus 6 on the new path. Redirects are clean (29/29 verified) — Google simply has not consolidated three days in. **Any page-level report for the next several weeks must union old and new URLs or it will read as a collapse that did not happen.**
49. **The C2.4 bet is validating before a single article of it has published.** `mas kahwin ikut negeri` position 11, `maskawin ikut negeri` 10, `maskawin setiap negeri` 10, `mas kawin wilayah persekutuan` 11 — all page one or two on the strength of one legacy post. Strengthens the case for opening the queue rather than holding it.
50. **Unclaimed demand found in the GSC data:** `dewan komuniti setiawangsa` 25 impressions at position 8.9 and `pusat komuniti setiawangsa` 21 at 9.5 — **zero clicks on either**. Venue-name queries landing on a generic listicle. Assigned to `head-of-seo-content` as a live, already-ranking opportunity rather than a new keyword bet.
51. **Backups escalated to urgent, and deliberately NOT dispatched during the deploy.** Production still has `pitr_enabled=false`, zero platform backups, and normal tooling that cannot produce a dump. Commissioned since 23 Aug (decision 28) and not returned. Sequencing call: an agent probing production Supabase while a production deploy is landing is bad practice, so this dispatches the moment the deploy is verified — not later, and not concurrently.

## 2026-08-24 — The Vercel token we already had

58. **⚠ CEO ERROR: the edge-purge work was never blocked on the owner.** The CEO spent the afternoon reporting "a Vercel API token in the vault" as the single outstanding owner request, and closed a board meeting on it. The owner said to check. **Vault key `vercel.twn` reaches team `thewednotebook` (`team_Mkofv56yM7EItimRjwSkiqNC`) and project `hellokahwin` (`prj_pGV0Cq7wrZZbCHq94DNYj89Urotj`) with write scope** — hellokahwin sits in the TWN Vercel team, so the TWN token covers it and no separate token exists or is needed. The CEO took the engineer's escalation ("needs a Vercel API token in the vault") as a statement about the company rather than about that agent's session, and never opened the `/tokens` registry. Ninety seconds of checking. **Root cause worth keeping: "an agent is blocked on a credential" and "the company lacks a credential" are different problems** — the first is fixed with `-PermissionMode`, the second with an owner request. Persona updated to diagnose which one is in play before spending the owner's attention.
59. **Branch filter APPLIED by the CEO directly**, proving the write scope in the same action. `commandForIgnoringBuildStep` went from `null` — nothing filtered, every branch building — to skipping `feat/command-centre-dashboard`. Decision 53 is now executed, not merely taken.
60. **Decision 45 (ingest-time edge purge) is UNBLOCKED and returns to the normal work queue.** It was never waiting on the owner.
61. **Registry corrected** so this cannot be rediscovered: `/tokens` now records the vault key, both ids, the working invocation (`--` confuses the param binder; bare `bash` resolves to WSL, use `-Cmd` with `pwsh`), that production ships through the git integration rather than the CLI, and the preview-env-var gap.

## 2026-08-24 — Late session: database access granted, R2 backup commissioned

52. **FULL PRODUCTION DATABASE CRUD GRANTED to the CEO and its agents** (owner, 24 Aug). Triggered by the deploy agent stopping mid-run: it had staged and validated the production probe, then hit session permission rules blocking the credential fetch. The CEO escalated rather than deciding, correctly — credentials/access is carve-out (1). Owner's answer: *"I want you to be granted this full access to CRUD the database."* **Database access is no longer a stopping point.** Carve-out (4) survives it: destructive operations with no recovery path — drops, truncates, unrollbackable migrations — still stop. Additive, reversible writes proceed on CEO authority, with a precise undo (the exact slugs or ids written) recorded first.
53. **Vercel branch filter applied — CEO decision, not escalated.** The integration was building `feat/command-centre-dashboard`, the migration-tool lineage with no `next` dependency, so every push there fired a doomed preview build. Narrow, reversible build-config change; none of the four carve-outs. Taken without asking, which is the point of decision 42.
54. **R2 database backup commissioned** — owner request (*"can we also plan out an R2 backup somewhere"*), and the right instinct. Basis: `hellokahwin-assets` already exists and is empty, master R2 credentials were rolled and fully verified 23 Aug (read/write/create/delete all confirmed), so **storage, credentials and access path are already solved** — only the dump and the schedule are missing. No new vendor, no new spend, no credential to wait for. Brief demands: solve the `pg_dump` 16.15 vs PostgreSQL 17.6 blocker and report what actually worked; **prove the backup by restoring it** (a dump nobody has restored is a hypothesis, and today's hand-rolled 23 Aug logical backup is "not a backup system, it is a souvenir"); push to `db-backups/YYYY/MM/DD/`; propose a schedule with an explicit credential story; state retention cost; and **build a loud failure alarm**, because a silently-stopped backup job is worse than none. Also commissioned: **the actual price of Supabase PITR for this tier** — if it is a few dollars, it is strictly better than a daily snapshot and R2 becomes the off-platform second copy rather than the primary defence.
55. **Sequencing held: backups dispatch AFTER the publish verifies, not concurrently.** Owner chose "publish now, backups right after" over "recovery point first". Reasoning accepted: the pending writes are additive inserts reversible by deleting slugs, and the `noindex` clock is real. Two agents writing to the same production database in the same window is also how you lose the ability to tell which one broke it.
56. **Process defect found and fixed mid-meeting: the skill prescribed the wrong watcher.** Step 3b told the CEO to use `orca terminal wait --for tui-idle`, which returns whenever the TUI goes momentarily quiet inside a long tool call. In this one session it falsely reported the deploy agent finished while it was mid-`git push`, and the SEO agent finished while its subagent was still running. The repo already shipped the correct tool — `watch-agent.sh`, polling `status-board.py`, which classifies DELEGATING separately from IDLE/DONE and STALLED. Skill corrected, plus a new standing rule: **verify facts directly** (`git rev-parse origin/master`, a curl status code, a GSC pull) rather than trusting an agent's account of its own work.
57. **Deploy confirmed landed by direct verification, not by report:** `origin/master` at `105d9de`, production pillars still 200, sitemap still 39 URLs through the push. This is the rule in 56 being applied to the very run that produced it.

## 2026-08-24 — Technical SEO round table, and the CEO decisions from it
36. **Convened `/bmad-party-mode`** with the in-house Head of SEO & Content alongside BMAD's Winston (architect), Amelia (dev), Murat (test architect) and Mary (analyst). **BMAD is now HelloKahwin's outsourced consultancy and development team**; the in-house Full-Stack Engineer role was RETIRED (persona preserved at `docs/boardroom/retired-agents/`). Owner directive.
37. **Murat's challenge — only ONE of 29 legacy redirects had ever been verified — was correct to raise, and the audit came back clean.** All 29 legacy WordPress URLs tested: **29/29 one hop to 200, zero multi-hop, zero broken.** The redirect layer is sound. Evidence: full loop over `data/hellokahwin-export/content/posts.json`, 24 Aug 2026.
38. **SITEMAP SUBMITTED to Search Console** — it never had been. Google downloaded it within the minute: **Valid, 39 URLs, 0 errors, 0 warnings.** Submitted BEFORE publishing the eight articles, deliberately: sitemaps are re-fetched continuously, so waiting bought nothing and cost discovery time during the migration window. Verified `lastmod` first — all 39 entries carry `2026-08-21`, the migration date, so Google gets a genuine recrawl signal rather than a decorative submission.
39. **⚠ Mary's finding: the plan's baseline measured URLs that no longer exist.** The 32 clicks / 2,163 impressions / position 20.6 figure was captured on the OLD WordPress URLs before the 21 Aug migration; the 30/60/90-day checkpoints built on it were comparing a dead structure to a live one. **Re-baselined on the new structure (21–24 Aug, 3 days): 7 clicks, 245 impressions, CTR 2.86%, position 16.9.** Normalised, that is ~2.3 clicks/day against ~1.1 before, with CTR nearly doubled and average position improved from 20.6 to 16.9. **The migration appears to have helped, not hurt** — though three days is a small sample and this is a directional read, not a verdict.
40. **Winston's finding: the noindex-until-populated rule has an expiry nobody set.** Empty hubs are correctly kept out of the sitemap as soft-404 protection, but Google degrades long-term `noindex` toward `nofollow` — so leaving seven pillars empty strands the cluster architecture at exactly the moment Google is redrawing its map of the site. This converts publishing the eight articles from a content decision into a technical one with a clock on it.
41. **Revalidate fix dispatched to BMAD via /autopilot** as the gate before publishing. Rationale, now doubly supported: Googlebot hitting a not-yet-materialised page on a brand-new URL is an unrecoverable first impression, and the noindex clock makes delay costly. Required proof: ingest a throwaway article, request it ONCE, show the literal status code — if it needs a second request it is not fixed.

## 2026-08-24 — SHIPPED: pillar pages live in production
30. **The deploy landed.** Six of seven pillar pages verified live against production: hantaran-mas-kahwin, ucapan-doa, busana-pengantin, pelamin-kad-cenderahati, venue-perancangan, sebelum-nikah — all HTTP 200. Slugs taken from `src/lib/inspire/pillars.ts` rather than guessed; an earlier CEO report of three 404s was wrong because the CEO invented the slugs.
31. **The redirect chain is fixed** — verified by counting responses, `/dewan-kahwin/` → 308 → `/artikel/idea-dan-nasihat/dewan-kahwin` → 200. One hop, down from two this morning.
32. **P1 `nikah-undang-undang` 404s** — three attempts, consistent. Its CHILDREN are live (`borang-pendaftaran-nikah` 200, `rukun-syarat-sah-nikah` 200), so the subtree seeded and only the parent pillar row is missing or malformed. One bad row, not a broken deploy — and it is the largest demand pool (~19,000 searches/mo). Dispatched for diagnosis before any change.
33. **CEO error worth recording: my `docs/` push triggered two failed Vercel builds.** Vercel's git integration is live on `ianngkb/hellokahwin` and built branch `feat/command-centre-dashboard` commit `0ba34c6` — the migration-tool lineage, no `next` dependency, so "No Next.js version detected". Preview only, failed at install, production untouched. Consequence: any push to that branch fires a doomed build. Needs the integration scoped or the branch ignored.
34. **The CLI deploy path was a dead end.** `vercel deploy --prod` from the worktree ran 16+ minutes and registered NO deployment; the CEO established this by querying Vercel directly rather than waiting on the agent. Every historical Ready Production build came through the git integration.
35. **A transient 504** appeared once on an article page and cleared on retry (three subsequent 200s). Logged for the engineer to check against function logs rather than dismissed as noise.

## 2026-08-23 — CEO closes Escalation 2 (owner-delegated)
26. **AUTHORIZED the production seed of ~33 `inspire_categories` rows.** The owner delegated this decision to the CEO. Basis: I read `scripts/seed-pillars.ts` before authorizing rather than approving an uninspected production write. Its header states "no renames, no reparenting, no deletes, ever"; it defaults to dry-run; the write is an idempotent upsert on `slug` touching only name/entity/updated_at on conflict. Additive reference data into one table holding 15 rows, reversible by deleting the new slugs, with a logical backup taken today. Weighed against the cost of not doing it — eight finished articles, seven pillar pages and a 26-cluster plan invisible indefinitely — the trade was not close. Conditions imposed: dry-run first with a stop-and-report if the plan is not ~33 inserts and zero destructive ops; dump the current table contents as a precise table-level undo before writing; that script only; verify with literal curl output against the live site.
27. **The agents' refusal to type `--i-know-this-is-remote` was correct and is endorsed.** A safety flag whose purpose is "a human confirmed this is production" is worthless if an agent types it under calendar pressure. Two engineers independently reserved it for a human; both were right.
28. **⚠ Production has NO recovery point** — `pitr_enabled=false`, zero platform backups, and the normal tooling could not even produce a dump (pg_dump 16.15 refuses PostgreSQL 17.6; the Supabase CLI path needs Docker, which is down). Today's protection is a hand-rolled logical backup. Commissioned as a follow-up investigation with options and cost; deliberately NOT blocking the seed, which is additive and reversible.
29. **Before-state recorded for verification:** all seven `/artikel/<pillar>` return 404; the live sitemap contains only `idea-dan-nasihat`, `real-wedding`, `uncategorized`; and the legacy redirect is TWO hops (`/dewan-kahwin/` → `/dewan-kahwin` → `/artikel/idea-dan-nasihat/dewan-kahwin` → 200). Any claim of success is measured against these.

## 2026-08-23 — Owner decisions on risk, rights and scope
20. **`docs/` committed and pushed to git.** 57 files — boardroom, plans, work-done, the eight article drafts — now on `origin/feat/command-centre-dashboard`. Basis: the entire company record existed only on local disk with no history or backup. Excluded deliberately: `.claude/agents/` (derived from skillcentral by install.sh; committing creates a drifting second source of truth), 2.8 MB of scratch gazette/SERP pages (evidence of fact-checking, not an input), and the generated dashboard. Note: this local repo shares a root commit with the live site repo — one repo, divergent lineages — so the push went to a branch, never master.
21. **The twelve live listicles carrying unprovable image rights: LEAVE UP.** Owner decision. No legal read commissioned; revisit only if someone complains.
22. **Real Wedding posts featuring identifiable couples: STAY UP** while licences are chased. Owner decision, taken with the PDPA and consent position on the record.
23. **Social accounts: still deferred.** Owner decision, despite content now flowing.
24. **Two CEO errors blocked the deploy, neither of them the code's fault.** (a) The review gate was scoped to the whole codebase rather than the diff, so 15 inherited `inspire-fixes` findings blocked a branch that touches none of those files. (b) The engineer was dispatched with its working directory set to the docs repo while the site code lives in an Orca worktree, so every file write was refused by the permission classifier — four attempts, two tools. Both corrected; dispatch now takes an explicit `-RepoPath`.
25. **AI authorship tag committed** (`7b8f5a7`): `article_authorship` + `article_review_status` enums, four columns, `articles_review_queue_idx`. The migration is hand-ordered on purpose — drizzle-kit's generated form emits `DEFAULT 'ai' NOT NULL` inside `ADD COLUMN`, which would have back-stamped all 29 legacy WordPress posts as AI-written.

## 2026-08-23 — Production deploy approved
15. **Pillar pages + content-ingest path + single-hop redirect fix APPROVED for production deploy.** Basis: built and verified locally by `full-stack-engineer` on branch `ianng89/pillars-ingest-redirects` (9 commits, never pushed, no preview or production deploy). Unblocks eight finished C2.4 articles currently held at stage 7 as orphans-in-waiting.
16. **AI authorship tag added to the same release** (owner directive: "tag it as AI in the articles section so we can manually review it later"). Article record gains `authorship` (ai / ai_assisted / human, not nullable, default ai for pipeline output) and `review_status` (pending_review / reviewed / needs_changes) plus reviewed_at/by. Surfaced in the articles admin view with an AI badge, filters, one-click mark-reviewed, and pending-first sort. **Internal review tracking only — not a public disclosure banner**, though the field is built so it could be surfaced publicly if the board later decides. Legacy 29 migrated posts tagged `human` unless evidence says otherwise.
17. **The eight C2.4 articles do NOT publish in this release.** They stay held until the pillar pages are live and verified; publishing them is a separate CEO decision.
18. **Review board validated on first use.** 27 blocks across 8 articles, every one raised by the verification seat, none traded against the run — including a fabricated quotation (newspaper lede + indirect speech stitched with an ellipsis, printed as a direct quote), a false premise the writer built a thesis on, an error originating in the CEO's own brief, and a non-existent RM45 fee that came out of our own "verified" table. Basis: `docs/work-done/aug-23-2026-session-01/aug-23-2026-done-cluster-01-production.md`.
19. **Cluster C2.4's competitive weapon, from primary sources:** six of fourteen Malaysian jurisdictions fix no minimum mas kahwin at all, and three figures dominating Google's page one (Perak RM101, Penang RM24, Kedah RM22.50) have no official backing anywhere. Reshaped five of eight briefs.

## 2026-08-23 — Cluster plan approved
12. **Cluster Launch Plan APPROVED by the board.** 26 clusters, 204 mapped topics, ~143,700 Malay searches/mo. Basis: Ahrefs `my` data pulled 23 Aug 2026 (14,892 units); rejections justified on seasonality, intent, SERP shape and topical radius. Supersedes the framework's topical map. Prediction: first movement in weeks on Tier 1 clusters (mas kahwin ikut negeri — already position 12.9 — hantaran kahwin, gubahan dulang hantaran); 60/90-day checkpoints are the scoreboard, 30-day is directional only because of the 21 Aug URL migration.
13. **ppsignature.com identified as the real competitor** — DR 4, 2,263 MY keywords, 1,493 top-three, ~29,745 visits/mo, from a blog attached to a dress shop; more than nikahsatu and TWN combined. Was absent from the Phase 1 competitor set. Basis: Ahrefs Site Explorer, index 2026-08-01. Consequence: the market is unclaimed rather than hard, and this site belongs in every future report.
14. **Two gaps opened by the board on approval**, dispatched to `head-of-seo-content`: (a) the production doctrine — how the writing system overtakes incumbents over time, compounding, counter-attack case, and a stop rule; (b) **visual assets — a genuine hole**: nothing in the framework or plan says where a single photograph comes from, against 204 topics requiring 1,000+ images. Includes a rights audit of the 682 inherited media items (ownership unverified) and a hire recommendation if warranted.

## 2026-08-23 — Founding check-in (planning continuation)
7. **Malay Topical Authority plan APPROVED (v3).** Target raised by the board: 32 → **1,500 clicks/28d by 21 Nov 2026**, **80 articles**, **2 writer hires** in Phase 2. Basis: GSC baseline (32 clicks, 2,163 imp, pos 20.6) + Ahrefs MY difficulty 0–2 on 370–2,000-volume Malay keywords. Prediction: 150 clicks @30d, 500 @60d, 1,500 @90d; article count is the leading indicator.
8. **Topical authority mastery gate added.** Board directive: the team masters the discipline (Ahrefs SERP research + reverse-engineering sites that own a topic) and writes the method permanently into its own persona BEFORE any audit work. Basis: we are betting company growth on this discipline.
9. **CEO does not do specialist work; all delegation runs in a VISIBLE terminal.** Basis: owner caught the CEO doing the SEO hire's keyword research. Persona and /hellokahwin skill updated; `dispatch-agent.ps1` added.
10. **Product correction — the site is already rebuilt.** hellokahwin.com runs Next.js on Vercel from repo `ianngkb/hellokahwin` (Drizzle → Supabase, DB-driven content); the local folder is the old Electron migration tool and the live repo is NOT cloned here. Consequence: no rebuild is needed, only a content-ingest path. Basis: response headers + GitHub inspection, 23 Aug 2026.
11. **Phase 1 dispatched** to `head-of-seo-content` in a visible terminal.

## 2026-08-23 — Founding check-in
1. **Data pipeline first** — no growth bets before a baseline. Basis: zero reachable metrics. Prediction: full organic baseline at next meeting via Ahrefs MCP + GSC MCP.
2. **Hired Head of SEO & Content** (`head-of-seo-content`). Basis: traffic north star; 29-post content base needs a topical-authority engine. Prediction: approved content framework + data-backed calendar by next meeting.
3. **/humanizer on all content** — company-wide rule. Basis: owner directive; no AI-sounding copy ships.
4. **Malay-first keyword research; topical authority; framework → bulk quality.** Basis: owner directive; Malay search demand is its own landscape.
5. **Social deferred** until content flows; revisit next meeting. Basis: no accounts; nothing to feed them yet.
6. **ceo-memory.md established** as the CEO's permanent product context. Basis: owner directive — know the product inside out.
