# CONT-08: eight C2.5 articles live in 259 seconds, cluster complete at 8/8

26 Aug 2026 · **Brief:** `docs/plans/aug-23-2026-session-01/aug-26-2026-brief-cont-08.md` (docs repo)
**Branch:** `ianng89/pillars-ingest-redirects` · **Worktree:** `orca/workspaces/hellokahwin-site/pillars-ingest-redirects`
**Undo:** `2026-08-26-publish-cont-08-c25-UNDO.md` + `.sql` + `undo-eight.mjs` — written, dry-run-proved and **committed (0098727) before the first write**.
**Evidence:** `2026-08-26-publish-cont-08-c25-EVIDENCE/` — pre-state, ingest transcript, undo dry run, every proof body, both sitemaps.

**The company log with the full story and the Stage 9 retrospective lives in the
docs repo:** `docs/work-done/aug-23-2026-session-01/aug-26-2026-done-cont-08-c25-nisbah.md`.

## The writes

```
pnpm --silent ingest <file> --db "$DB" --commit --publish --revalidate-url https://hellokahwin.com
```

Eight files in dependency order (each links only to earlier siblings), eight
exit 0, `RUN START 15:33:34Z`, `RUN END 15:37:53Z`. `$DB` read from `.env`
inside the runner; `VERCEL_TOKEN` injected by `vault.ps1 run`; edge purged
after every write (article + pillar + sitemap, HTTP 200 each).

```
articles      61 -> 69        media    884 -> 911 (+27, one per declared image)
published     61 -> 69        tags      76 ->  90 (+14 new, 4 pre-existing reused)
sitemap       78 -> 86        P2 pillar 13 ->  21    cluster C2.5   0 -> 8
```

## Proof (serial sweep, 4s apart, bodies kept)

All eight cold renders `200 / MISS / age=0` at 103–130 KB (the degraded-hub
signature is ~29 KB), no robots meta, cover credit joined exactly on
`media.url = articles.cover_image_url` on all eight, pillar page links to all
eight, sitemap 86 with all eight present. `published_at` of the eight live
articles this batch links to: byte-identical before and after. Zero text-card
media on the eight.

GSC: the runner had no GSC credential (its warning is in the transcript), so
the sitemap was resubmitted through the `gsc` connector at 15:40Z —
"Status: Pending processing".

## Ship gate

`pnpm --silent audit:drafts` over both draft dirs: **all eight at LIVE=DRAFT,
0 missing, 0 extra.** The same run lists 17 OTHER published articles whose
drafts disagree with production — pre-existing CONT-09 drift, direction
reversed (production is ahead: the re-selected covers were written to the DB,
the drafts still declare the old ones). **An `--update` from any of those 17
drafts would revert CONT-09's cover re-selection.** Raised in the company log;
not touched by this run.
