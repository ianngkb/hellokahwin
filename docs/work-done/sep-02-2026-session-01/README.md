# Sprint 06 — "Deepen where the click is" · work-done index

Session `sep-02-2026-session-01`. One entry per item, newest last. Each entry
carries its own before/after evidence in a sibling `*-EVIDENCE/` directory.

**Append your item here when you ship it.** Several agents work this sprint in
separate worktrees, so this file is a merge point by design — resolve conflicts by
keeping both rows, never by replacing the table.

| Item        | Title                                                                    | Log                                                                                          | Exit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- | --- |
| **UI-20**   | The favicon was a pink H from a palette that is in no file in this repo   | [`sep-02-2026-done-ui-20-favicon-monogram.md`](./sep-02-2026-done-ui-20-favicon-monogram.md) | **The HK monogram, ink on parchment, live.** Item gate `0/7 → 7/7` against production (`FAVICON GATE EXIT: 1 → 0`). `/favicon.ico` 404 → **200** (16/32/48 entries), `/icon.svg` 404 → **200 image/svg+xml**, `/apple-icon.png` 404 → **200 180×180**. Homepage icon links **1 → 5**, all three rel values. Raster favicon dominant `#b4326e` (82.5%) → **`#edeae1` = `--hk-parchment-100` (84.0%)**, **0 px** of the retired magenta. `favicon-32.png` **deleted**. 16px render check on the live ICO: 2 ink groups, 2 clear columns between them, crossbar 0.38, K swing 2px, 11.08:1. Gates shipped: `pnpm brand:icons:check`, `pnpm audit:favicon`. CORRECTION TO THE BRIEF: `#b4326e` is in **no** palette file in this repo, retired or current — Plum Forward's plum is `oklch(0.22 0.055 310)`. The icon never came from a palette; nothing generated it and nothing checked it |

| **PLAT-16** | A soft-failed pillar render is served, and cached, as an empty topic hub | [`sep-02-2026-done-plat-16-degraded-render-not-cached.md`](./sep-02-2026-done-plat-16-degraded-render-not-cached.md) | **Both content reads on `/artikel/[category]` now fail the render instead of rendering an empty page.** Forced for real with `LOCK TABLE articles IN ACCESS EXCLUSIVE MODE`: pillar `200 3800ms "Panduan ini masih kosong"x2` → **`500 3120ms`**, grid `200 3075ms "Kategori ini masih kosong"x2` → **`500 3044ms`**, and the next request after recovery serves the real page (**4** and **15** article links, zero empty markers). CORRECTION TO THE DoD: `revalidate = false` is **inert** here — the route awaits `searchParams`, `next build` prints `ƒ /artikel/[category]` with no Revalidate/Expire, and every response carries `Cache-Control: private, no-cache, no-store`. There is no ISR entry and `revalidateTag` was never what would clear it; what persisted the empty page is `Vercel-CDN-Cache-Control: s-maxage=300, stale-while-revalidate=600` — **fifteen minutes at the edge, not indefinite**. GRID ROUTE: same exposure, same file, fixed here. FOUND IN REVIEW and fixed: the first fix threw at **7,704ms against `maxDuration = 5`**, i.e. after Vercel kills the function — one shared budget derived from the ceiling brings it to 3,120ms. Gates: `pnpm verify:degraded` (assertion **C** red at 7,138ms, green at 3,120ms), 4 route tests in 2 pairs, `category-render-budget.test.ts`. Reviewer: **Claude**, two adversarial passes. SHIPPED: PR #62 (0f2a4c9) merged, then PR #66 (9c1ca0c) because Vercel moved to **pnpm 11** mid-sprint and every deploy in the repo was failing (`ERR_PNPM_IGNORED_BUILDS`; the setting had ALSO been renamed `onlyBuiltDependencies` -> `allowBuilds`, so the obvious fix failed with the byte-identical error). Production deployment 6209298019 **success** — but CORRECTED AT SOURCE: reading the pnpm version out of the last EIGHT production builds shows only ONE (`0f2a4c99`, the failure) ever got 11.x and the other seven got 10.28.0, so PR #66 did NOT unblock the deploy; selection moved back on its own. The `allowBuilds` change is kept as the defence for the next 11.x selection, not claimed as the cause of the green. LIVE 200s with 47/19/34 article links and zero empty-state markers on three hubs; positive control HelloKahwin x52; negative control /artikel/plat16-not-a-real-category -> 404 |
| **CONT-15** | Portrait article covers: the count re-derived, and the depiction check a green gate cannot do | [`sep-02-2026-done-cont-15-portrait-covers.md`](./sep-02-2026-done-cont-15-portrait-covers.md) | **The defect is closed on production — by UI-16, not by this item, and this entry says so.** Re-derived at run time, NOT the carried "12 of 86": **14 of 92** covers failed the 25% ceiling (`0.667` x8, `0.748`, `0.750` x4, `0.753`). **The corpus then moved 92 -> 96 -> 97 -> 102 in under two hours** while this item ran — +10, exactly the ten CONT-17/CONT-18 were dispatched to add, landing live underneath the measurement. CORRECTION TO THE BRIEF: the failing slot was **not** the `.s-row` thumbnail — DES-18's `crop-4x3-article-card-sm` is on all 92 rows at 528x396 and renders at **0.00%** deviation. It was the article cover figure, a fixed `aspect-[3/2]` box fed `low.webp`. Named case `tempat-beli-hantaran`: `image-aspect 125% off, ~44% of the frame kept` at all five widths -> **`image-aspect 0`, `image-upscale 0`, `shaped-slot-variant 0`** on live production, verified independently from computed values. CONT-15's own plate is **superseded and NOT merged** — `.hk-cover-plate` sets an explicit CSS `aspect-ratio` and is fed `low`, exactly what UI-16's new blocking `shaped-slot-variant` check forbids; the spec is recorded as superseded rather than rewritten to match. **THE FINDING THIS ITEM EXISTS FOR:** every instrument went green and **three of the fifteen re-cut portrait covers stopped depicting their subject** — `baju-pengantin-sewa-atau-beli` (an article about the BAJU, cropped to a beauty headshot with the garment out of frame), `doa-selamat-majlis` (an imam reading, cropped to a lattice screen), `hantaran-tunang-3-balas-5` (a ring in a box, cropped to a band of beads with the ring outside the frame) — while **five came out BETTER**, two unreadable signboards made readable. All fifteen retain **50.0-56.4%**, clear of UI-03's ~one-third floor: retention measures AREA, depiction is about WHERE THE SUBJECT SITS, and no threshold separates them. Shipped: `pnpm audit:crops` (the contact sheet, both refusal paths run), `scripts/backfill-cover-intrinsics.mts` (**96 production rows**, additive, undo pushed, verified idempotent with `sizeBytes` intact on every row), and **`playwright-core` as a devDependency — the gate this item's DoD names has never been installable from a clean checkout**. Reviewer: **Claude**, two adversarial passes plus independent Creative Director re-verification |
## Findings carried in from Sprint 05, resolved here

| Finding | Raised by | Status |
| --- | --- | --- |
| `scripts/measure/count-in-html.sh`, cited in the standing rules and in the `design-systems-engineer` persona, does not exist on `master`. | PLAT-19, confirmed by DES-18 | **CLOSED.** Added by UI-17 in `f109450`; used by UI-20 against the live `/brand` page. |
| `docs/work-done/README.md`, named in the standing rules, does not exist. | PLAT-19, confirmed by DES-18 | **CLOSED by UI-20.** Created as a real index over the per-session indexes, rather than reported a third time. |

## Open findings raised by this session, with their owners

| Finding | Raised by | Owner |
| --- | --- | --- |
| **`gh auth status` is not a reliable statement of which token `gh` will use.** It reported `ianngkb` as `Active account: true` while `gh api user --jq .login` returned `ianng89`, whose token carries `pull` only — `gh pr create` failed with `GraphQL: must be a collaborator`. `gh auth switch --hostname github.com --user ianngkb` fixed it. Any agent shipping a PR should verify with `gh api user`, not `gh auth status`. | UI-20 | platform / CEO |
| **The brief's mechanism was wrong even though its verdict was right.** UI-20's magenta was described as "a survivor of the palette the site retired"; it is in **no** palette file in this repo, current or retired. The two diagnoses imply different fixes ("re-skin it too" vs "build a generator and a gate"). Worth tightening how a dispatch states provenance it has not grepped. | UI-20 | CEO |
| **`public/hellokahwin-logo.png` CARRIES THE SAME RETIRED MAGENTA, and it is on every social share card.** Measured, not suspected: 886×290, 663 distinct colours, **7,127 px of the exact `#b4326e`**, 5,446 px of a plum `#3d2b3d`, on an off-white `#faf7f2` that is not `--hk-parchment-100` either. It is the `openGraph`/`twitter` image on `/artikel`, `/artikel/[category]`, `/artikel/tag/[slug]` and `/artikel/author/[slug]`, and the `logo` in the homepage **Organization schema** and the article schema — so the colour UI-20 just removed from the tab strip is still what Facebook, WhatsApp, X and Google are handed. **Outside UI-20's DoD, which is scoped to the favicon and app-icon set — raised, not silently absorbed.** The fix is the same shape as UI-20's and can reuse `scripts/generate-brand-icons.mjs`: derive it from `hellokahwin-horizontal.svg` + tokens. | UI-20 | design-systems-engineer / CEO to scope |
| **Three live covers do not depict their subject, and every automated check calls them fine.** `baju-pengantin-sewa-atau-beli` (an article about renting or buying the **baju** — the crop keeps the face and cuts the garment out of frame), `doa-selamat-majlis` (an imam reading before a congregation, now a lattice screen with the top of a songkok at the bottom edge), `hantaran-tunang-3-balas-5` (a ring in a decorated box, now a band of gold beads with the ring outside the frame). All three retain **50.0%**, above UI-03's ~one-third floor; `image-aspect`, `image-upscale`, `image-attr-aspect` and `shaped-slot-variant` are all **0** on them. The fix is a re-cut of three focal points, **not** a change to UI-16's route — five of the same fifteen came out *better* than their sources. Evidence: `cont-15-EVIDENCE/portrait-covers-crop-4x3-md-contact-sheet.png`; regenerate with `pnpm audit:crops --crop crop-4x3-article-card-md --portrait-only`. | CONT-15 | creative-director / CEO to schedule |
| **Two items were dispatched onto the same DOM element in the same sprint and neither knew.** UI-16 and CONT-15 both correctly diagnosed the article cover figure, both measured production the same day, and shipped opposite answers — one keeps the design's box and re-cuts the photograph, the other keeps the photograph and moves the box. The collision was invisible until one merged; the other had a green branch one command from overruling shipped art direction. Nothing in the sprint tooling surfaces "these two items touch the same selector". | CONT-15 | CEO / sprint tooling |
| **`scripts/ui-layout-gate.mjs` imports `playwright-core` and it has never been a declared dependency.** `git log -S playwright-core -- package.json` returns nothing. The gate named in several DoDs could not run from a clean checkout — everyone who has run it had the package lying around from something else. **Fixed here** (PR #69), recorded because a gate that only runs on machines with the right leftovers is not a gate. | CONT-15 | closed by CONT-15 |
| **The layout gate buffers its entire report to the end**, so a long `--url` sweep writes zero bytes for its whole run and is indistinguishable from a hang. A 107-URL sitewide sweep was killed at ~55 minutes for looking dead. The workaround is chunking the URL list; printing per-target as each finishes would be the fix. | CONT-15 | design-systems-engineer |
| **`ImageVariantMeta` has no dimensions, and no ingest path fills the ones CONT-15 backfilled.** `low.width`/`low.height` are recorded on 96 production rows and were **already 6 rows stale twenty minutes later** (`102 published · 6 to measure`). Both admin actions replace `coverImageVariants` wholesale, so a re-uploaded cover loses them. Not urgent while nothing reads the field; a trap the day something does. | CONT-15 | design-systems-engineer |
| **This worktree is shared, and another session hard-reset `HEAD` mid-item**, discarding uncommitted working-tree edits between two commits on the same branch and reparenting one of them onto the wrong base. Recorded because the standing rules warn about the shared *stash* but say nothing about the shared *index and HEAD*. The safe pattern is to build the commit with plumbing (`git commit-tree`) and push the object, rather than to stage from the working tree. | CONT-15 | CEO / platform |
