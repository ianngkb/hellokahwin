# Ahrefs audit, Phase 3 — work-done index

Session `sep-04-2026-session-01`. One entry per item, newest last. Each entry
that makes a production write carries a sibling `*-UNDO.md`, pushed before the
write runs.

**Append your item here when you ship it.** Several agents work this audit in
separate worktrees, so this file is a merge point by design — resolve conflicts
by keeping both rows, never by replacing the table.

| Item | Title | Log | Exit |
| --- | --- | --- | --- |
| **D24** | The 369 photo-essay images nobody had described | [`sep-04-2026-done-d24-vision-alt.md`](./sep-04-2026-done-d24-vision-alt.md) · [UNDO](./sep-04-2026-done-d24-vision-alt-UNDO.md) | **369 images described, 0 left on the positional fallback, verified against live production.** Phase 2 filled every empty `alt` but 369 reached its last rung as `${title} — gambar ${n}` — the same sentence 43 times down a page of 43 different photographs. A vision pass opened each image at the `mid.webp` rung (all 369 confirmed present, ≤ 350 KB) with the article's own prose as the only source a venue or vendor name could come from; no personal names, even where a place card spelled one out. 63–115 chars, mean 92, Malay, unique within each article. `humanizer` over every batch and again over a 40-row sample of the final text, which caught a terminal-full-stop split across six articles vs eight (171 trimmed), `menjulang` used figuratively, three display verbs, two `lengkap dengan` fillers, and `baju melayu` against the corpus's `Baju Melayu`. Targets are found by recomputing `fallbackImageAlt(title, ordinal)` and matching it EXACTLY, so the walk moved into `_content-images.mts` and `backfill-image-alt.mts` now shares it — two walkers that disagreed would hand a photograph a description of a different one. Each description carries the `src` it was written from and the apply refuses a mismatch, because `id` + `ordinal` names a POSITION and a position can be handed a different picture. Every rule fails the whole run, never one row. REVIEW: 3 layers on Opus 5 + scoped fix-check, **20 findings, all fixed, none deferred**, verdict `clean`. The two that mattered: `--apply` computed a fresh manifest, discarded it, and wrote only what the stored one named, so descriptions appended after the dry run were silently never written while it reported "APPLIED — N rows updated"; and a gallery entry that was null or a bare string threw and took the whole migration with it. CHECKED, NOT ASSUMED: two ordinal rules changed after 369 alts had already been written against the old ones — across all 102 published articles there are zero src-less figures and zero galleries, and the extract picks a byte-identical target set before and after. Backup `articles_backup_20260904t155750z_visionalt` (102 rows); badge/authorship counts identical before and after; `updated_at` moves on 14 rows and the UNDO says why |
