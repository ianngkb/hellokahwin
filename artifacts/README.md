# artifacts/ — committed deliverables

Permanent outputs for this project (reports, dashboards, research, ship
records): one folder per deliverable, `YYYY-MM-DD-<slug>/` with a short
README. Scratch goes to `tmp/` (ignored). Rules: `../brain/docs/artifacts.md`
(or `../../brain/...` from a nested checkout).
